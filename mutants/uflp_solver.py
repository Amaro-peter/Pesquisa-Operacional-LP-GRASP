"""
Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem (UFLP)

Based on the framework by Resende & Werneck (2006):
"A hybrid multistart heuristic for the uncapacitated facility location problem"

Improvement: Uses the LP relaxation (solved with SciPy's HiGHS backend) to bias
the GRASP construction phase,
replacing uniform random selection with probability-weighted selection derived
from exact fractional solutions.

Usage:
    python uflp_solver.py                              # Random instance demo
    python uflp_solver.py --file cap71.txt             # OR-Library instance
    python uflp_solver.py --n-facilities 25 --n-customers 40 --seed 7
"""

from __future__ import annotations

import argparse
import math
import random
import sys
import time
from dataclasses import dataclass, field
from typing import Dict, List, Set, Tuple

import numpy as np
from scipy.optimize import linprog
import scipy.sparse as sp


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict

# ============================================================================
# Section 1: Data Structures
# ============================================================================

@dataclass
class UFLPInstance:
    """Problem instance for the Uncapacitated Facility Location Problem."""
    facilities: List[int]
    customers: List[int]
    setup_costs: Dict[int, float]
    service_costs: Dict[int, Dict[int, float]]  # service_costs[customer][facility]


@dataclass
class SolutionState:
    """Algorithmic state tracking an incumbent solution and auxiliary data."""
    open_facilities: Set[int] = field(default_factory=set)
    total_cost: float = float('inf')
    closest_facility: Dict[int, int] = field(default_factory=dict)
    second_closest_facility: Dict[int, int] = field(default_factory=dict)
    save: Dict[int, float] = field(default_factory=dict)
    loss: Dict[int, float] = field(default_factory=dict)
    extra: Dict[Tuple[int, int], float] = field(default_factory=dict)
mutants_x_parse_orlib_instance__mutmut: MutantDict = {}  # type: ignore


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

@_mutmut_mutated(mutants_x_parse_orlib_instance__mutmut)
def parse_orlib_instance(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_orig(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_1(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(None, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_2(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, None) as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_3(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open('r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_4(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, ) as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_5(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'XXrXX') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_6(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'R') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_7(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = None

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_8(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = None
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_9(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 1
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_10(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = None; idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_11(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(None); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_12(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx = 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_13(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx -= 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_14(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 2
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_15(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = None; idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_16(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(None); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_17(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx = 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_18(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx -= 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_19(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 2

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_20(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = None
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_21(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(None)
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_22(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(None))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_23(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = None
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_24(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(None)
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_25(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(None))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_26(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = None
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_27(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = None

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_28(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = None; idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_29(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(None); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_30(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx = 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_31(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx -= 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_32(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 2  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_33(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = None; idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_34(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(None); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_35(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx = 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_36(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx -= 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_37(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 2

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_38(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = None; idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_39(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(None); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_40(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx = 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_41(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx -= 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_42(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 2  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_43(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = None; idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_44(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(None); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_45(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx = 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_46(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx -= 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_47(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 2
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_48(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = None

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_49(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=None,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_50(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=None,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_51(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=None,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_52(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=None,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_53(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_54(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_55(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        service_costs=service_costs,
    )


# ============================================================================
# Section 2: Instance I/O
# ============================================================================

def x_parse_orlib_instance__mutmut_56(filepath: str) -> UFLPInstance:
    """
    Parse a UFLP instance in OR-Library format (Beasley).

    Format:
        Line 1: n_facilities  n_customers
        Next n_facilities lines: capacity  setup_cost  (one per facility)
        Then n_customers blocks, each:
            demand
            n_facilities service costs (may span multiple lines)
        (capacity and demand are ignored for the uncapacitated version)
    """
    with open(filepath, 'r') as f:
        tokens = f.read().split()

    idx = 0
    n_facilities = int(tokens[idx]); idx += 1
    n_customers = int(tokens[idx]); idx += 1

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))
    setup_costs: Dict[int, float] = {}
    service_costs: Dict[int, Dict[int, float]] = {u: {} for u in customers}

    # Read facility data: capacity, setup_cost
    for fac in facilities:
        _capacity = float(tokens[idx]); idx += 1  # ignored
        setup_costs[fac] = float(tokens[idx]); idx += 1

    # Read customer data: demand, then n_facilities service costs
    for cust in customers:
        _demand = float(tokens[idx]); idx += 1  # ignored
        for fac in facilities:
            cost = float(tokens[idx]); idx += 1
            service_costs[cust][fac] = cost

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        )

mutants_x_parse_orlib_instance__mutmut['_mutmut_orig'] = x_parse_orlib_instance__mutmut_orig # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_1'] = x_parse_orlib_instance__mutmut_1 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_2'] = x_parse_orlib_instance__mutmut_2 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_3'] = x_parse_orlib_instance__mutmut_3 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_4'] = x_parse_orlib_instance__mutmut_4 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_5'] = x_parse_orlib_instance__mutmut_5 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_6'] = x_parse_orlib_instance__mutmut_6 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_7'] = x_parse_orlib_instance__mutmut_7 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_8'] = x_parse_orlib_instance__mutmut_8 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_9'] = x_parse_orlib_instance__mutmut_9 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_10'] = x_parse_orlib_instance__mutmut_10 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_11'] = x_parse_orlib_instance__mutmut_11 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_12'] = x_parse_orlib_instance__mutmut_12 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_13'] = x_parse_orlib_instance__mutmut_13 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_14'] = x_parse_orlib_instance__mutmut_14 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_15'] = x_parse_orlib_instance__mutmut_15 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_16'] = x_parse_orlib_instance__mutmut_16 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_17'] = x_parse_orlib_instance__mutmut_17 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_18'] = x_parse_orlib_instance__mutmut_18 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_19'] = x_parse_orlib_instance__mutmut_19 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_20'] = x_parse_orlib_instance__mutmut_20 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_21'] = x_parse_orlib_instance__mutmut_21 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_22'] = x_parse_orlib_instance__mutmut_22 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_23'] = x_parse_orlib_instance__mutmut_23 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_24'] = x_parse_orlib_instance__mutmut_24 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_25'] = x_parse_orlib_instance__mutmut_25 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_26'] = x_parse_orlib_instance__mutmut_26 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_27'] = x_parse_orlib_instance__mutmut_27 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_28'] = x_parse_orlib_instance__mutmut_28 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_29'] = x_parse_orlib_instance__mutmut_29 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_30'] = x_parse_orlib_instance__mutmut_30 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_31'] = x_parse_orlib_instance__mutmut_31 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_32'] = x_parse_orlib_instance__mutmut_32 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_33'] = x_parse_orlib_instance__mutmut_33 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_34'] = x_parse_orlib_instance__mutmut_34 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_35'] = x_parse_orlib_instance__mutmut_35 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_36'] = x_parse_orlib_instance__mutmut_36 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_37'] = x_parse_orlib_instance__mutmut_37 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_38'] = x_parse_orlib_instance__mutmut_38 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_39'] = x_parse_orlib_instance__mutmut_39 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_40'] = x_parse_orlib_instance__mutmut_40 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_41'] = x_parse_orlib_instance__mutmut_41 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_42'] = x_parse_orlib_instance__mutmut_42 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_43'] = x_parse_orlib_instance__mutmut_43 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_44'] = x_parse_orlib_instance__mutmut_44 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_45'] = x_parse_orlib_instance__mutmut_45 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_46'] = x_parse_orlib_instance__mutmut_46 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_47'] = x_parse_orlib_instance__mutmut_47 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_48'] = x_parse_orlib_instance__mutmut_48 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_49'] = x_parse_orlib_instance__mutmut_49 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_50'] = x_parse_orlib_instance__mutmut_50 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_51'] = x_parse_orlib_instance__mutmut_51 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_52'] = x_parse_orlib_instance__mutmut_52 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_53'] = x_parse_orlib_instance__mutmut_53 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_54'] = x_parse_orlib_instance__mutmut_54 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_55'] = x_parse_orlib_instance__mutmut_55 # type: ignore # mutmut generated
mutants_x_parse_orlib_instance__mutmut['x_parse_orlib_instance__mutmut_56'] = x_parse_orlib_instance__mutmut_56 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_generate_random_instance__mutmut)
def generate_random_instance(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_orig(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_1(
    n_facilities: int = 21,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_2(
    n_facilities: int = 20,
    n_customers: int = 31,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_3(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 43,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_4(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = None

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_5(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(None)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_6(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = None
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_7(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(None)
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_8(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(None))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_9(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = None

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_10(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(None)

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_11(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(None))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_12(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = None
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_13(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(None, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_14(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, None), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_15(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_16(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, ), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_17(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(1, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_18(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1001), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_19(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(None, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_20(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, None)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_21(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_22(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, )) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_23(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(1, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_24(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1001)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_25(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = None

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_26(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(None, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_27(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, None), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_28(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_29(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, ), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_30(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(1, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_31(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1001), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_32(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(None, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_33(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, None)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_34(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_35(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, )) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_36(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(1, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_37(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1001)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_38(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = None

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_39(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(None, 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_40(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), None) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_41(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_42(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), ) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_43(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(None, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_44(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, None), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_45(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_46(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, ), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_47(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(101, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_48(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5001), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_49(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 3) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_50(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = None
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_51(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = None
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_52(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = None
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_53(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = None
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_54(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = None
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_55(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt(None)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_56(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 - (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_57(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) * 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_58(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux + fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_59(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 3 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_60(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) * 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_61(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy + fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_62(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 3)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_63(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = None

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_64(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(None, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_65(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, None)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_66(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_67(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, )

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_68(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 5)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_69(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=None,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_70(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=None,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_71(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=None,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_72(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        service_costs=None,
    )


def x_generate_random_instance__mutmut_73(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        customers=customers,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_74(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        setup_costs=setup_costs,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_75(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        service_costs=service_costs,
    )


def x_generate_random_instance__mutmut_76(
    n_facilities: int = 20,
    n_customers: int = 30,
    seed: int = 42,
) -> UFLPInstance:
    """
    Generate a random Euclidean UFLP instance.

    Facilities and customers are placed uniformly in [0, 1000]².
    Setup costs are drawn uniformly from [100, 5000].
    Service costs equal the Euclidean distance.
    """
    rng = random.Random(seed)

    facilities = list(range(n_facilities))
    customers = list(range(n_customers))

    fac_coords = {f: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for f in facilities}
    cust_coords = {u: (rng.uniform(0, 1000), rng.uniform(0, 1000)) for u in customers}

    setup_costs = {f: round(rng.uniform(100, 5000), 2) for f in facilities}

    service_costs: Dict[int, Dict[int, float]] = {}
    for u in customers:
        service_costs[u] = {}
        ux, uy = cust_coords[u]
        for f in facilities:
            fx, fy = fac_coords[f]
            dist = math.sqrt((ux - fx) ** 2 + (uy - fy) ** 2)
            service_costs[u][f] = round(dist, 4)

    return UFLPInstance(
        facilities=facilities,
        customers=customers,
        setup_costs=setup_costs,
        )

mutants_x_generate_random_instance__mutmut['_mutmut_orig'] = x_generate_random_instance__mutmut_orig # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_1'] = x_generate_random_instance__mutmut_1 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_2'] = x_generate_random_instance__mutmut_2 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_3'] = x_generate_random_instance__mutmut_3 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_4'] = x_generate_random_instance__mutmut_4 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_5'] = x_generate_random_instance__mutmut_5 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_6'] = x_generate_random_instance__mutmut_6 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_7'] = x_generate_random_instance__mutmut_7 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_8'] = x_generate_random_instance__mutmut_8 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_9'] = x_generate_random_instance__mutmut_9 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_10'] = x_generate_random_instance__mutmut_10 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_11'] = x_generate_random_instance__mutmut_11 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_12'] = x_generate_random_instance__mutmut_12 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_13'] = x_generate_random_instance__mutmut_13 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_14'] = x_generate_random_instance__mutmut_14 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_15'] = x_generate_random_instance__mutmut_15 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_16'] = x_generate_random_instance__mutmut_16 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_17'] = x_generate_random_instance__mutmut_17 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_18'] = x_generate_random_instance__mutmut_18 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_19'] = x_generate_random_instance__mutmut_19 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_20'] = x_generate_random_instance__mutmut_20 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_21'] = x_generate_random_instance__mutmut_21 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_22'] = x_generate_random_instance__mutmut_22 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_23'] = x_generate_random_instance__mutmut_23 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_24'] = x_generate_random_instance__mutmut_24 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_25'] = x_generate_random_instance__mutmut_25 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_26'] = x_generate_random_instance__mutmut_26 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_27'] = x_generate_random_instance__mutmut_27 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_28'] = x_generate_random_instance__mutmut_28 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_29'] = x_generate_random_instance__mutmut_29 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_30'] = x_generate_random_instance__mutmut_30 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_31'] = x_generate_random_instance__mutmut_31 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_32'] = x_generate_random_instance__mutmut_32 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_33'] = x_generate_random_instance__mutmut_33 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_34'] = x_generate_random_instance__mutmut_34 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_35'] = x_generate_random_instance__mutmut_35 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_36'] = x_generate_random_instance__mutmut_36 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_37'] = x_generate_random_instance__mutmut_37 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_38'] = x_generate_random_instance__mutmut_38 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_39'] = x_generate_random_instance__mutmut_39 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_40'] = x_generate_random_instance__mutmut_40 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_41'] = x_generate_random_instance__mutmut_41 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_42'] = x_generate_random_instance__mutmut_42 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_43'] = x_generate_random_instance__mutmut_43 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_44'] = x_generate_random_instance__mutmut_44 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_45'] = x_generate_random_instance__mutmut_45 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_46'] = x_generate_random_instance__mutmut_46 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_47'] = x_generate_random_instance__mutmut_47 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_48'] = x_generate_random_instance__mutmut_48 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_49'] = x_generate_random_instance__mutmut_49 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_50'] = x_generate_random_instance__mutmut_50 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_51'] = x_generate_random_instance__mutmut_51 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_52'] = x_generate_random_instance__mutmut_52 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_53'] = x_generate_random_instance__mutmut_53 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_54'] = x_generate_random_instance__mutmut_54 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_55'] = x_generate_random_instance__mutmut_55 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_56'] = x_generate_random_instance__mutmut_56 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_57'] = x_generate_random_instance__mutmut_57 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_58'] = x_generate_random_instance__mutmut_58 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_59'] = x_generate_random_instance__mutmut_59 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_60'] = x_generate_random_instance__mutmut_60 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_61'] = x_generate_random_instance__mutmut_61 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_62'] = x_generate_random_instance__mutmut_62 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_63'] = x_generate_random_instance__mutmut_63 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_64'] = x_generate_random_instance__mutmut_64 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_65'] = x_generate_random_instance__mutmut_65 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_66'] = x_generate_random_instance__mutmut_66 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_67'] = x_generate_random_instance__mutmut_67 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_68'] = x_generate_random_instance__mutmut_68 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_69'] = x_generate_random_instance__mutmut_69 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_70'] = x_generate_random_instance__mutmut_70 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_71'] = x_generate_random_instance__mutmut_71 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_72'] = x_generate_random_instance__mutmut_72 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_73'] = x_generate_random_instance__mutmut_73 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_74'] = x_generate_random_instance__mutmut_74 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_75'] = x_generate_random_instance__mutmut_75 # type: ignore # mutmut generated
mutants_x_generate_random_instance__mutmut['x_generate_random_instance__mutmut_76'] = x_generate_random_instance__mutmut_76 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut: MutantDict = {}  # type: ignore


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

@_mutmut_mutated(mutants_x_solve_lp_relaxation__mutmut)
def solve_lp_relaxation(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_orig(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_1(
    instance: UFLPInstance,
    verbose: bool = False,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_2(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = None
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_3(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = None
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_4(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = None
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_5(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = None
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_6(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = None
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_7(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(None)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_8(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = None
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_9(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(None)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_10(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = None
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_11(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F - n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_12(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U / n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_13(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = None
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_14(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(None)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_15(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = None
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_16(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = None
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_17(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F - f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_18(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F - u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_19(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] / n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_20(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = None
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_21(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = None
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_22(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (1.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_23(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 2.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_24(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = None
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_25(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = None
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_26(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(None)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_27(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(None)
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_28(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F - f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_29(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F - u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_30(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx / n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_31(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(None)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_32(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(2.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_33(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = None
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_34(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix(None, shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_35(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=None)
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_36(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix(shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_37(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), )
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_38(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = None
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_39(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(None)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_40(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = None
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_41(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U / n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_42(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = None
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_43(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = None
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_44(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 1
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_45(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = None
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_46(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = None
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_47(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(None)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_48(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(None)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_49(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F - f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_50(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F - u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_51(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx / n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_52(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(None)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_53(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(2.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_54(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(None)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_55(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(None)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_56(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(None)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_57(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(+1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_58(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-2.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_59(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx = 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_60(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx -= 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_61(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 2
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_62(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = None
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_63(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix(None, shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_64(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=None)
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_65(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix(shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_66(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), )
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_67(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = None
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_68(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(None)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_69(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = None
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_70(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(None, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_71(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=None, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_72(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=None, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_73(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=None, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_74(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=None, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_75(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=None, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_76(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method=None)
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_77(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_78(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_79(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_80(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_81(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_82(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_83(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, )
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_84(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds] / n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_85(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='XXhighsXX')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_86(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='HIGHS')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_87(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_88(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(None)
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_89(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = None

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_90(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = None
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_91(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = None
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_92(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(None)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_93(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(2 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_94(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 1.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_95(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 <= v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_96(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v <= 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_97(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 1.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_98(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = None
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_99(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(None)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_100(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(2 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_101(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v > 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_102(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 1.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_103(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(None)
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_104(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(None)
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_105(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(None)
        print(f"          Facilities fractional: {n_frac}/{len(F_list)}")

    return lp_probs


# ============================================================================
# Section 3: Phase 1 — Simplex-Based LP Relaxation
# ============================================================================

def x_solve_lp_relaxation__mutmut_106(
    instance: UFLPInstance,
    verbose: bool = True,
) -> Dict[int, float]:
    """
    Solve the LP relaxation of the UFLP with SciPy's `linprog` (HiGHS backend).

    Variables:
        y[f] ∈ [0, 1]  — fraction of facility f opened
        x[u][f] ∈ [0, 1] — fraction of customer u served by facility f

    Constraints:
        Σ_f x[u][f] = 1               for each customer u  (full assignment)
        x[u][f] ≤ y[f]                for each (u, f)       (linking)

    Returns:
        Dict mapping each facility to its fractional LP value y[f].
    """
    F_list = instance.facilities
    U_list = instance.customers
    n_F = len(F_list)
    n_U = len(U_list)
    
    f_map = {f: i for i, f in enumerate(F_list)}
    u_map = {u: i for i, u in enumerate(U_list)}
    
    n_vars = n_F + n_U * n_F
    
    c_obj = np.zeros(n_vars)
    for f in F_list:
        c_obj[f_map[f]] = instance.setup_costs[f]
    for u in U_list:
        for f in F_list:
            idx = n_F + u_map[u] * n_F + f_map[f]
            c_obj[idx] = instance.service_costs[u][f]
            
    bounds = (0.0, 1.0)
    
    A_eq_row, A_eq_col, A_eq_data = [], [], []
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            A_eq_row.append(u_idx)
            A_eq_col.append(n_F + u_idx * n_F + f_map[f])
            A_eq_data.append(1.0)
    A_eq = sp.csc_matrix((A_eq_data, (A_eq_row, A_eq_col)), shape=(n_U, n_vars))
    b_eq = np.ones(n_U)
    
    n_ineq = n_U * n_F
    A_ub_row, A_ub_col, A_ub_data = [], [], []
    
    row_idx = 0
    for u in U_list:
        u_idx = u_map[u]
        for f in F_list:
            f_idx = f_map[f]
            # x[u][f]
            A_ub_row.append(row_idx)
            A_ub_col.append(n_F + u_idx * n_F + f_idx)
            A_ub_data.append(1.0)
            
            # -y[f]
            A_ub_row.append(row_idx)
            A_ub_col.append(f_idx)
            A_ub_data.append(-1.0)
            
            row_idx += 1
            
    A_ub = sp.csc_matrix((A_ub_data, (A_ub_row, A_ub_col)), shape=(n_ineq, n_vars))
    b_ub = np.zeros(n_ineq)
    
    res = linprog(c_obj, A_ub=A_ub, b_ub=b_ub, A_eq=A_eq, b_eq=b_eq, bounds=[bounds]*n_vars, method='highs')
    
    if not res.success:
        raise RuntimeError(f"LP relaxation did not reach optimality (status={res.status}): {res.message}")
        
    lp_probs: Dict[int, float] = {f: res.x[f_map[f]] for f in F_list}

    if verbose:
        lp_obj = res.fun
        n_frac = sum(1 for v in lp_probs.values() if 0.01 < v < 0.99)
        n_integral = sum(1 for v in lp_probs.values() if v >= 0.99)
        print(f"[Phase 1] LP Relaxation solved")
        print(f"          LP objective (lower bound): {lp_obj:,.2f}")
        print(f"          Facilities integral (y>=0.99): {n_integral}/{len(F_list)}")
        print(None)

    return lp_probs

mutants_x_solve_lp_relaxation__mutmut['_mutmut_orig'] = x_solve_lp_relaxation__mutmut_orig # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_1'] = x_solve_lp_relaxation__mutmut_1 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_2'] = x_solve_lp_relaxation__mutmut_2 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_3'] = x_solve_lp_relaxation__mutmut_3 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_4'] = x_solve_lp_relaxation__mutmut_4 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_5'] = x_solve_lp_relaxation__mutmut_5 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_6'] = x_solve_lp_relaxation__mutmut_6 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_7'] = x_solve_lp_relaxation__mutmut_7 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_8'] = x_solve_lp_relaxation__mutmut_8 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_9'] = x_solve_lp_relaxation__mutmut_9 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_10'] = x_solve_lp_relaxation__mutmut_10 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_11'] = x_solve_lp_relaxation__mutmut_11 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_12'] = x_solve_lp_relaxation__mutmut_12 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_13'] = x_solve_lp_relaxation__mutmut_13 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_14'] = x_solve_lp_relaxation__mutmut_14 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_15'] = x_solve_lp_relaxation__mutmut_15 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_16'] = x_solve_lp_relaxation__mutmut_16 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_17'] = x_solve_lp_relaxation__mutmut_17 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_18'] = x_solve_lp_relaxation__mutmut_18 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_19'] = x_solve_lp_relaxation__mutmut_19 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_20'] = x_solve_lp_relaxation__mutmut_20 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_21'] = x_solve_lp_relaxation__mutmut_21 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_22'] = x_solve_lp_relaxation__mutmut_22 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_23'] = x_solve_lp_relaxation__mutmut_23 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_24'] = x_solve_lp_relaxation__mutmut_24 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_25'] = x_solve_lp_relaxation__mutmut_25 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_26'] = x_solve_lp_relaxation__mutmut_26 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_27'] = x_solve_lp_relaxation__mutmut_27 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_28'] = x_solve_lp_relaxation__mutmut_28 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_29'] = x_solve_lp_relaxation__mutmut_29 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_30'] = x_solve_lp_relaxation__mutmut_30 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_31'] = x_solve_lp_relaxation__mutmut_31 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_32'] = x_solve_lp_relaxation__mutmut_32 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_33'] = x_solve_lp_relaxation__mutmut_33 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_34'] = x_solve_lp_relaxation__mutmut_34 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_35'] = x_solve_lp_relaxation__mutmut_35 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_36'] = x_solve_lp_relaxation__mutmut_36 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_37'] = x_solve_lp_relaxation__mutmut_37 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_38'] = x_solve_lp_relaxation__mutmut_38 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_39'] = x_solve_lp_relaxation__mutmut_39 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_40'] = x_solve_lp_relaxation__mutmut_40 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_41'] = x_solve_lp_relaxation__mutmut_41 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_42'] = x_solve_lp_relaxation__mutmut_42 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_43'] = x_solve_lp_relaxation__mutmut_43 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_44'] = x_solve_lp_relaxation__mutmut_44 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_45'] = x_solve_lp_relaxation__mutmut_45 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_46'] = x_solve_lp_relaxation__mutmut_46 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_47'] = x_solve_lp_relaxation__mutmut_47 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_48'] = x_solve_lp_relaxation__mutmut_48 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_49'] = x_solve_lp_relaxation__mutmut_49 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_50'] = x_solve_lp_relaxation__mutmut_50 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_51'] = x_solve_lp_relaxation__mutmut_51 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_52'] = x_solve_lp_relaxation__mutmut_52 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_53'] = x_solve_lp_relaxation__mutmut_53 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_54'] = x_solve_lp_relaxation__mutmut_54 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_55'] = x_solve_lp_relaxation__mutmut_55 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_56'] = x_solve_lp_relaxation__mutmut_56 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_57'] = x_solve_lp_relaxation__mutmut_57 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_58'] = x_solve_lp_relaxation__mutmut_58 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_59'] = x_solve_lp_relaxation__mutmut_59 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_60'] = x_solve_lp_relaxation__mutmut_60 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_61'] = x_solve_lp_relaxation__mutmut_61 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_62'] = x_solve_lp_relaxation__mutmut_62 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_63'] = x_solve_lp_relaxation__mutmut_63 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_64'] = x_solve_lp_relaxation__mutmut_64 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_65'] = x_solve_lp_relaxation__mutmut_65 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_66'] = x_solve_lp_relaxation__mutmut_66 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_67'] = x_solve_lp_relaxation__mutmut_67 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_68'] = x_solve_lp_relaxation__mutmut_68 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_69'] = x_solve_lp_relaxation__mutmut_69 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_70'] = x_solve_lp_relaxation__mutmut_70 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_71'] = x_solve_lp_relaxation__mutmut_71 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_72'] = x_solve_lp_relaxation__mutmut_72 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_73'] = x_solve_lp_relaxation__mutmut_73 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_74'] = x_solve_lp_relaxation__mutmut_74 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_75'] = x_solve_lp_relaxation__mutmut_75 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_76'] = x_solve_lp_relaxation__mutmut_76 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_77'] = x_solve_lp_relaxation__mutmut_77 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_78'] = x_solve_lp_relaxation__mutmut_78 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_79'] = x_solve_lp_relaxation__mutmut_79 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_80'] = x_solve_lp_relaxation__mutmut_80 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_81'] = x_solve_lp_relaxation__mutmut_81 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_82'] = x_solve_lp_relaxation__mutmut_82 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_83'] = x_solve_lp_relaxation__mutmut_83 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_84'] = x_solve_lp_relaxation__mutmut_84 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_85'] = x_solve_lp_relaxation__mutmut_85 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_86'] = x_solve_lp_relaxation__mutmut_86 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_87'] = x_solve_lp_relaxation__mutmut_87 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_88'] = x_solve_lp_relaxation__mutmut_88 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_89'] = x_solve_lp_relaxation__mutmut_89 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_90'] = x_solve_lp_relaxation__mutmut_90 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_91'] = x_solve_lp_relaxation__mutmut_91 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_92'] = x_solve_lp_relaxation__mutmut_92 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_93'] = x_solve_lp_relaxation__mutmut_93 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_94'] = x_solve_lp_relaxation__mutmut_94 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_95'] = x_solve_lp_relaxation__mutmut_95 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_96'] = x_solve_lp_relaxation__mutmut_96 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_97'] = x_solve_lp_relaxation__mutmut_97 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_98'] = x_solve_lp_relaxation__mutmut_98 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_99'] = x_solve_lp_relaxation__mutmut_99 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_100'] = x_solve_lp_relaxation__mutmut_100 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_101'] = x_solve_lp_relaxation__mutmut_101 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_102'] = x_solve_lp_relaxation__mutmut_102 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_103'] = x_solve_lp_relaxation__mutmut_103 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_104'] = x_solve_lp_relaxation__mutmut_104 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_105'] = x_solve_lp_relaxation__mutmut_105 # type: ignore # mutmut generated
mutants_x_solve_lp_relaxation__mutmut['x_solve_lp_relaxation__mutmut_106'] = x_solve_lp_relaxation__mutmut_106 # type: ignore # mutmut generated
mutants_x__find_closest_two__mutmut: MutantDict = {}  # type: ignore


# ============================================================================
# Section 4: Phase 2 — Probabilistic Constructive Heuristic
# ============================================================================

@_mutmut_mutated(mutants_x__find_closest_two__mutmut)
def _find_closest_two(
    customer: int,
    open_set: Set[int],
    service_costs: Dict[int, Dict[int, float]],
) -> Tuple[int, int]:
    """Return (closest_facility, second_closest_facility) for a customer."""
    costs = service_costs[customer]
    sorted_facs = sorted(open_set, key=lambda f: costs[f])
    closest = sorted_facs[0]
    second = sorted_facs[1] if len(sorted_facs) > 1 else closest
    return closest, second


# ============================================================================
# Section 4: Phase 2 — Probabilistic Constructive Heuristic
# ============================================================================

def x__find_closest_two__mutmut_orig(
    customer: int,
    open_set: Set[int],
    service_costs: Dict[int, Dict[int, float]],
) -> Tuple[int, int]:
    """Return (closest_facility, second_closest_facility) for a customer."""
    costs = service_costs[customer]
    sorted_facs = sorted(open_set, key=lambda f: costs[f])
    closest = sorted_facs[0]
    second = sorted_facs[1] if len(sorted_facs) > 1 else closest
    return closest, second


# ============================================================================
# Section 4: Phase 2 — Probabilistic Constructive Heuristic
# ============================================================================

def x__find_closest_two__mutmut_1(
    customer: int,
    open_set: Set[int],
    service_costs: Dict[int, Dict[int, float]],
) -> Tuple[int, int]:
    """Return (closest_facility, second_closest_facility) for a customer."""
    costs = None
    sorted_facs = sorted(open_set, key=lambda f: costs[f])
    closest = sorted_facs[0]
    second = sorted_facs[1] if len(sorted_facs) > 1 else closest
    return closest, second


# ============================================================================
# Section 4: Phase 2 — Probabilistic Constructive Heuristic
# ============================================================================

def x__find_closest_two__mutmut_2(
    customer: int,
    open_set: Set[int],
    service_costs: Dict[int, Dict[int, float]],
) -> Tuple[int, int]:
    """Return (closest_facility, second_closest_facility) for a customer."""
    costs = service_costs[customer]
    sorted_facs = None
    closest = sorted_facs[0]
    second = sorted_facs[1] if len(sorted_facs) > 1 else closest
    return closest, second


# ============================================================================
# Section 4: Phase 2 — Probabilistic Constructive Heuristic
# ============================================================================

def x__find_closest_two__mutmut_3(
    customer: int,
    open_set: Set[int],
    service_costs: Dict[int, Dict[int, float]],
) -> Tuple[int, int]:
    """Return (closest_facility, second_closest_facility) for a customer."""
    costs = service_costs[customer]
    sorted_facs = sorted(None, key=lambda f: costs[f])
    closest = sorted_facs[0]
    second = sorted_facs[1] if len(sorted_facs) > 1 else closest
    return closest, second


# ============================================================================
# Section 4: Phase 2 — Probabilistic Constructive Heuristic
# ============================================================================

def x__find_closest_two__mutmut_4(
    customer: int,
    open_set: Set[int],
    service_costs: Dict[int, Dict[int, float]],
) -> Tuple[int, int]:
    """Return (closest_facility, second_closest_facility) for a customer."""
    costs = service_costs[customer]
    sorted_facs = sorted(open_set, key=None)
    closest = sorted_facs[0]
    second = sorted_facs[1] if len(sorted_facs) > 1 else closest
    return closest, second


# ============================================================================
# Section 4: Phase 2 — Probabilistic Constructive Heuristic
# ============================================================================

def x__find_closest_two__mutmut_5(
    customer: int,
    open_set: Set[int],
    service_costs: Dict[int, Dict[int, float]],
) -> Tuple[int, int]:
    """Return (closest_facility, second_closest_facility) for a customer."""
    costs = service_costs[customer]
    sorted_facs = sorted(key=lambda f: costs[f])
    closest = sorted_facs[0]
    second = sorted_facs[1] if len(sorted_facs) > 1 else closest
    return closest, second


# ============================================================================
# Section 4: Phase 2 — Probabilistic Constructive Heuristic
# ============================================================================

def x__find_closest_two__mutmut_6(
    customer: int,
    open_set: Set[int],
    service_costs: Dict[int, Dict[int, float]],
) -> Tuple[int, int]:
    """Return (closest_facility, second_closest_facility) for a customer."""
    costs = service_costs[customer]
    sorted_facs = sorted(open_set, )
    closest = sorted_facs[0]
    second = sorted_facs[1] if len(sorted_facs) > 1 else closest
    return closest, second


# ============================================================================
# Section 4: Phase 2 — Probabilistic Constructive Heuristic
# ============================================================================

def x__find_closest_two__mutmut_7(
    customer: int,
    open_set: Set[int],
    service_costs: Dict[int, Dict[int, float]],
) -> Tuple[int, int]:
    """Return (closest_facility, second_closest_facility) for a customer."""
    costs = service_costs[customer]
    sorted_facs = sorted(open_set, key=lambda f: None)
    closest = sorted_facs[0]
    second = sorted_facs[1] if len(sorted_facs) > 1 else closest
    return closest, second


# ============================================================================
# Section 4: Phase 2 — Probabilistic Constructive Heuristic
# ============================================================================

def x__find_closest_two__mutmut_8(
    customer: int,
    open_set: Set[int],
    service_costs: Dict[int, Dict[int, float]],
) -> Tuple[int, int]:
    """Return (closest_facility, second_closest_facility) for a customer."""
    costs = service_costs[customer]
    sorted_facs = sorted(open_set, key=lambda f: costs[f])
    closest = None
    second = sorted_facs[1] if len(sorted_facs) > 1 else closest
    return closest, second


# ============================================================================
# Section 4: Phase 2 — Probabilistic Constructive Heuristic
# ============================================================================

def x__find_closest_two__mutmut_9(
    customer: int,
    open_set: Set[int],
    service_costs: Dict[int, Dict[int, float]],
) -> Tuple[int, int]:
    """Return (closest_facility, second_closest_facility) for a customer."""
    costs = service_costs[customer]
    sorted_facs = sorted(open_set, key=lambda f: costs[f])
    closest = sorted_facs[1]
    second = sorted_facs[1] if len(sorted_facs) > 1 else closest
    return closest, second


# ============================================================================
# Section 4: Phase 2 — Probabilistic Constructive Heuristic
# ============================================================================

def x__find_closest_two__mutmut_10(
    customer: int,
    open_set: Set[int],
    service_costs: Dict[int, Dict[int, float]],
) -> Tuple[int, int]:
    """Return (closest_facility, second_closest_facility) for a customer."""
    costs = service_costs[customer]
    sorted_facs = sorted(open_set, key=lambda f: costs[f])
    closest = sorted_facs[0]
    second = None
    return closest, second


# ============================================================================
# Section 4: Phase 2 — Probabilistic Constructive Heuristic
# ============================================================================

def x__find_closest_two__mutmut_11(
    customer: int,
    open_set: Set[int],
    service_costs: Dict[int, Dict[int, float]],
) -> Tuple[int, int]:
    """Return (closest_facility, second_closest_facility) for a customer."""
    costs = service_costs[customer]
    sorted_facs = sorted(open_set, key=lambda f: costs[f])
    closest = sorted_facs[0]
    second = sorted_facs[2] if len(sorted_facs) > 1 else closest
    return closest, second


# ============================================================================
# Section 4: Phase 2 — Probabilistic Constructive Heuristic
# ============================================================================

def x__find_closest_two__mutmut_12(
    customer: int,
    open_set: Set[int],
    service_costs: Dict[int, Dict[int, float]],
) -> Tuple[int, int]:
    """Return (closest_facility, second_closest_facility) for a customer."""
    costs = service_costs[customer]
    sorted_facs = sorted(open_set, key=lambda f: costs[f])
    closest = sorted_facs[0]
    second = sorted_facs[1] if len(sorted_facs) >= 1 else closest
    return closest, second


# ============================================================================
# Section 4: Phase 2 — Probabilistic Constructive Heuristic
# ============================================================================

def x__find_closest_two__mutmut_13(
    customer: int,
    open_set: Set[int],
    service_costs: Dict[int, Dict[int, float]],
) -> Tuple[int, int]:
    """Return (closest_facility, second_closest_facility) for a customer."""
    costs = service_costs[customer]
    sorted_facs = sorted(open_set, key=lambda f: costs[f])
    closest = sorted_facs[0]
    second = sorted_facs[1] if len(sorted_facs) > 2 else closest
    return closest, second

mutants_x__find_closest_two__mutmut['_mutmut_orig'] = x__find_closest_two__mutmut_orig # type: ignore # mutmut generated
mutants_x__find_closest_two__mutmut['x__find_closest_two__mutmut_1'] = x__find_closest_two__mutmut_1 # type: ignore # mutmut generated
mutants_x__find_closest_two__mutmut['x__find_closest_two__mutmut_2'] = x__find_closest_two__mutmut_2 # type: ignore # mutmut generated
mutants_x__find_closest_two__mutmut['x__find_closest_two__mutmut_3'] = x__find_closest_two__mutmut_3 # type: ignore # mutmut generated
mutants_x__find_closest_two__mutmut['x__find_closest_two__mutmut_4'] = x__find_closest_two__mutmut_4 # type: ignore # mutmut generated
mutants_x__find_closest_two__mutmut['x__find_closest_two__mutmut_5'] = x__find_closest_two__mutmut_5 # type: ignore # mutmut generated
mutants_x__find_closest_two__mutmut['x__find_closest_two__mutmut_6'] = x__find_closest_two__mutmut_6 # type: ignore # mutmut generated
mutants_x__find_closest_two__mutmut['x__find_closest_two__mutmut_7'] = x__find_closest_two__mutmut_7 # type: ignore # mutmut generated
mutants_x__find_closest_two__mutmut['x__find_closest_two__mutmut_8'] = x__find_closest_two__mutmut_8 # type: ignore # mutmut generated
mutants_x__find_closest_two__mutmut['x__find_closest_two__mutmut_9'] = x__find_closest_two__mutmut_9 # type: ignore # mutmut generated
mutants_x__find_closest_two__mutmut['x__find_closest_two__mutmut_10'] = x__find_closest_two__mutmut_10 # type: ignore # mutmut generated
mutants_x__find_closest_two__mutmut['x__find_closest_two__mutmut_11'] = x__find_closest_two__mutmut_11 # type: ignore # mutmut generated
mutants_x__find_closest_two__mutmut['x__find_closest_two__mutmut_12'] = x__find_closest_two__mutmut_12 # type: ignore # mutmut generated
mutants_x__find_closest_two__mutmut['x__find_closest_two__mutmut_13'] = x__find_closest_two__mutmut_13 # type: ignore # mutmut generated
mutants_x__compute_total_cost__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__compute_total_cost__mutmut)
def _compute_total_cost(
    instance: UFLPInstance,
    state: SolutionState,
) -> float:
    """Compute total cost = Σ setup + Σ service (to closest open facility)."""
    cost = sum(instance.setup_costs[f] for f in state.open_facilities)
    for u in instance.customers:
        cost += instance.service_costs[u][state.closest_facility[u]]
    return cost


def x__compute_total_cost__mutmut_orig(
    instance: UFLPInstance,
    state: SolutionState,
) -> float:
    """Compute total cost = Σ setup + Σ service (to closest open facility)."""
    cost = sum(instance.setup_costs[f] for f in state.open_facilities)
    for u in instance.customers:
        cost += instance.service_costs[u][state.closest_facility[u]]
    return cost


def x__compute_total_cost__mutmut_1(
    instance: UFLPInstance,
    state: SolutionState,
) -> float:
    """Compute total cost = Σ setup + Σ service (to closest open facility)."""
    cost = None
    for u in instance.customers:
        cost += instance.service_costs[u][state.closest_facility[u]]
    return cost


def x__compute_total_cost__mutmut_2(
    instance: UFLPInstance,
    state: SolutionState,
) -> float:
    """Compute total cost = Σ setup + Σ service (to closest open facility)."""
    cost = sum(None)
    for u in instance.customers:
        cost += instance.service_costs[u][state.closest_facility[u]]
    return cost


def x__compute_total_cost__mutmut_3(
    instance: UFLPInstance,
    state: SolutionState,
) -> float:
    """Compute total cost = Σ setup + Σ service (to closest open facility)."""
    cost = sum(instance.setup_costs[f] for f in state.open_facilities)
    for u in instance.customers:
        cost = instance.service_costs[u][state.closest_facility[u]]
    return cost


def x__compute_total_cost__mutmut_4(
    instance: UFLPInstance,
    state: SolutionState,
) -> float:
    """Compute total cost = Σ setup + Σ service (to closest open facility)."""
    cost = sum(instance.setup_costs[f] for f in state.open_facilities)
    for u in instance.customers:
        cost -= instance.service_costs[u][state.closest_facility[u]]
    return cost

mutants_x__compute_total_cost__mutmut['_mutmut_orig'] = x__compute_total_cost__mutmut_orig # type: ignore # mutmut generated
mutants_x__compute_total_cost__mutmut['x__compute_total_cost__mutmut_1'] = x__compute_total_cost__mutmut_1 # type: ignore # mutmut generated
mutants_x__compute_total_cost__mutmut['x__compute_total_cost__mutmut_2'] = x__compute_total_cost__mutmut_2 # type: ignore # mutmut generated
mutants_x__compute_total_cost__mutmut['x__compute_total_cost__mutmut_3'] = x__compute_total_cost__mutmut_3 # type: ignore # mutmut generated
mutants_x__compute_total_cost__mutmut['x__compute_total_cost__mutmut_4'] = x__compute_total_cost__mutmut_4 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__compute_auxiliary_data__mutmut)
def _compute_auxiliary_data(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_orig(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_1(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = None
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_2(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = None
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_3(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = None
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_4(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = None
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_5(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = None
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_6(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = None
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_7(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_8(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = None
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_9(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = None

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_10(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = None
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_11(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = None
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_12(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = +c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_13(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = None
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_14(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] + d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_15(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit = max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_16(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit -= max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_17(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(None, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_18(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, None)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_19(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_20(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, )
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_21(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(1.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_22(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = None

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_23(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = None
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_24(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = None  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_25(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = +c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_26(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] != f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_27(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty = d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_28(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty -= d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_29(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] + d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_30(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = None

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_31(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = None
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_32(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = None
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_33(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 1.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_34(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] != f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_35(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] <= d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_36(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction = (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_37(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction -= (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_38(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) + max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_39(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] + d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_40(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(None, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_41(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, None)
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_42(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_43(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, )
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_44(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(1, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_45(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] + d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_46(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(None) > 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_47(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) >= 1e-12:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_48(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1.000000000001:
                state.extra[(f_i, f_r)] = correction


def x__compute_auxiliary_data__mutmut_49(
    instance: UFLPInstance,
    state: SolutionState,
) -> None:
    """
    Compute save, loss, and extra from scratch per Resende & Werneck.

    Definitions (all with respect to the CURRENT solution state):

    save[f_i] (f_i closed): the cost DECREASE from inserting f_i.
        save[f_i] = -c(f_i) + Sum_u max(0, d(u, phi1[u]) - d(u, f_i))
        where phi1[u] = closest open facility to u.
        Positive save means inserting f_i improves the solution.

    loss[f_r] (f_r open): the cost INCREASE from removing f_r.
        loss[f_r] = -c(f_r) + Sum_{u: phi1[u]=f_r} (d(u, phi2[u]) - d(u, f_r))
        where phi2[u] = second closest open facility to u.
        Positive loss means removing f_r worsens the solution.
        (We save the setup cost c(f_r) but pay more in reassignment.)

    extra[(f_i, f_r)] (f_i closed, f_r open): swap correction.
        For users u with phi1[u] = f_r, if they would be reassigned to
        phi2[u] upon removal of f_r, but f_i is actually closer than phi2[u],
        then extra corrects for this. Specifically:
        extra[(f_i, f_r)] = Sum_{u: phi1[u]=f_r, d(u,f_i) < d(u,phi2[u])}
                                (d(u, phi2[u]) - d(u, f_i))

    Move profits:
        profit(insert f_i)          = save[f_i]
        profit(remove f_r)          = -loss[f_r]
        profit(swap f_i for f_r)    = save[f_i] - loss[f_r] + extra[(f_i, f_r)]

    IMPORTANT: The swap formula double-counts for customers assigned to f_r
    that would also benefit from f_i. The save already counts them (they get
    closer to f_i than phi1[u]=f_r), and loss already counts them (reassigning
    to phi2[u]). The extra term corrects loss's overestimate but save still
    includes the f_i benefit for those users. To fix this, we subtract from
    save the contribution of users assigned to f_r when computing swap profit.
    We store this per-pair correction in a separate field.
    """
    F = instance.facilities
    U = instance.customers
    c = instance.setup_costs
    d = instance.service_costs
    open_facs = state.open_facilities
    closed_facs = [f for f in F if f not in open_facs]
    phi1 = state.closest_facility
    phi2 = state.second_closest_facility

    # --- save[f_i] for each closed facility ---
    state.save = {}
    for f_i in closed_facs:
        benefit = -c[f_i]
        for u in U:
            gain = d[u][phi1[u]] - d[u][f_i]
            benefit += max(0.0, gain)
        state.save[f_i] = benefit

    # --- loss[f_r] for each open facility ---
    state.loss = {}
    for f_r in open_facs:
        penalty = -c[f_r]  # removing f_r saves setup cost (reduces loss)
        for u in U:
            if phi1[u] == f_r:
                penalty += d[u][phi2[u]] - d[u][f_r]
        state.loss[f_r] = penalty

    # --- extra[(f_i, f_r)] sparse ---
    state.extra = {}
    for f_i in closed_facs:
        for f_r in open_facs:
            correction = 0.0
            for u in U:
                if phi1[u] == f_r:
                    # Formula to perfectly correct the double counting of save and loss:
                    # extra_u = d(u, phi2[u]) - min(d(u, f_i), d(u, phi2[u])) - max(0, d(u, f_r) - d(u, f_i))
                    # It is only non-zero when d(u, f_i) < d(u, phi2[u])
                    if d[u][f_i] < d[u][phi2[u]]:
                        correction += (d[u][phi2[u]] - d[u][f_i]) - max(0, d[u][f_r] - d[u][f_i])
            if abs(correction) > 1e-12:
                state.extra[(f_i, f_r)] = None

mutants_x__compute_auxiliary_data__mutmut['_mutmut_orig'] = x__compute_auxiliary_data__mutmut_orig # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_1'] = x__compute_auxiliary_data__mutmut_1 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_2'] = x__compute_auxiliary_data__mutmut_2 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_3'] = x__compute_auxiliary_data__mutmut_3 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_4'] = x__compute_auxiliary_data__mutmut_4 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_5'] = x__compute_auxiliary_data__mutmut_5 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_6'] = x__compute_auxiliary_data__mutmut_6 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_7'] = x__compute_auxiliary_data__mutmut_7 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_8'] = x__compute_auxiliary_data__mutmut_8 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_9'] = x__compute_auxiliary_data__mutmut_9 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_10'] = x__compute_auxiliary_data__mutmut_10 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_11'] = x__compute_auxiliary_data__mutmut_11 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_12'] = x__compute_auxiliary_data__mutmut_12 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_13'] = x__compute_auxiliary_data__mutmut_13 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_14'] = x__compute_auxiliary_data__mutmut_14 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_15'] = x__compute_auxiliary_data__mutmut_15 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_16'] = x__compute_auxiliary_data__mutmut_16 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_17'] = x__compute_auxiliary_data__mutmut_17 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_18'] = x__compute_auxiliary_data__mutmut_18 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_19'] = x__compute_auxiliary_data__mutmut_19 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_20'] = x__compute_auxiliary_data__mutmut_20 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_21'] = x__compute_auxiliary_data__mutmut_21 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_22'] = x__compute_auxiliary_data__mutmut_22 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_23'] = x__compute_auxiliary_data__mutmut_23 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_24'] = x__compute_auxiliary_data__mutmut_24 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_25'] = x__compute_auxiliary_data__mutmut_25 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_26'] = x__compute_auxiliary_data__mutmut_26 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_27'] = x__compute_auxiliary_data__mutmut_27 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_28'] = x__compute_auxiliary_data__mutmut_28 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_29'] = x__compute_auxiliary_data__mutmut_29 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_30'] = x__compute_auxiliary_data__mutmut_30 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_31'] = x__compute_auxiliary_data__mutmut_31 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_32'] = x__compute_auxiliary_data__mutmut_32 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_33'] = x__compute_auxiliary_data__mutmut_33 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_34'] = x__compute_auxiliary_data__mutmut_34 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_35'] = x__compute_auxiliary_data__mutmut_35 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_36'] = x__compute_auxiliary_data__mutmut_36 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_37'] = x__compute_auxiliary_data__mutmut_37 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_38'] = x__compute_auxiliary_data__mutmut_38 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_39'] = x__compute_auxiliary_data__mutmut_39 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_40'] = x__compute_auxiliary_data__mutmut_40 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_41'] = x__compute_auxiliary_data__mutmut_41 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_42'] = x__compute_auxiliary_data__mutmut_42 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_43'] = x__compute_auxiliary_data__mutmut_43 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_44'] = x__compute_auxiliary_data__mutmut_44 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_45'] = x__compute_auxiliary_data__mutmut_45 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_46'] = x__compute_auxiliary_data__mutmut_46 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_47'] = x__compute_auxiliary_data__mutmut_47 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_48'] = x__compute_auxiliary_data__mutmut_48 # type: ignore # mutmut generated
mutants_x__compute_auxiliary_data__mutmut['x__compute_auxiliary_data__mutmut_49'] = x__compute_auxiliary_data__mutmut_49 # type: ignore # mutmut generated
mutants_x__compute_swap_profit__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__compute_swap_profit__mutmut)
def _compute_swap_profit(
    instance: UFLPInstance,
    state: SolutionState,
    f_i: int,
    f_r: int,
) -> float:
    """
    Compute the EXACT profit of swapping f_r (open) with f_i (closed) in O(1).
    We use the precomputed save, loss, and extra arrays, but adjust for the
    double-counting of customers currently assigned to f_r who also benefit from f_i.
    """
    # If there's only 1 open facility, we can't use loss (it assumes phi2 is a valid backup)
    if len(state.open_facilities) == 1:
        d = instance.service_costs
        delta = instance.setup_costs[f_i] - instance.setup_costs[f_r]
        for u in instance.customers:
            delta += d[u][f_i] - d[u][f_r]
        return -delta

    profit = state.save[f_i] - state.loss[f_r] + state.extra.get((f_i, f_r), 0.0)
    
    # Correct the double-counting: for any customer where phi1 == f_r,
    # save[f_i] already included max(0, d(u, f_r) - d(u, f_i)).
    # We must subtract this out because the f_r facility is being removed,
    # and their true reassignment is handled by loss + extra.
    # To do this in O(1) time without looping over all users, we would need 
    # to store this specific overlap. Since the current state doesn't have it,
    # we can store it in another sparse dict, or we can just compute the correction 
    # during _compute_auxiliary_data. Let's assume we update _compute_auxiliary_data
    # to include this overlap subtraction directly in `extra`.
    return profit


def x__compute_swap_profit__mutmut_orig(
    instance: UFLPInstance,
    state: SolutionState,
    f_i: int,
    f_r: int,
) -> float:
    """
    Compute the EXACT profit of swapping f_r (open) with f_i (closed) in O(1).
    We use the precomputed save, loss, and extra arrays, but adjust for the
    double-counting of customers currently assigned to f_r who also benefit from f_i.
    """
    # If there's only 1 open facility, we can't use loss (it assumes phi2 is a valid backup)
    if len(state.open_facilities) == 1:
        d = instance.service_costs
        delta = instance.setup_costs[f_i] - instance.setup_costs[f_r]
        for u in instance.customers:
            delta += d[u][f_i] - d[u][f_r]
        return -delta

    profit = state.save[f_i] - state.loss[f_r] + state.extra.get((f_i, f_r), 0.0)
    
    # Correct the double-counting: for any customer where phi1 == f_r,
    # save[f_i] already included max(0, d(u, f_r) - d(u, f_i)).
    # We must subtract this out because the f_r facility is being removed,
    # and their true reassignment is handled by loss + extra.
    # To do this in O(1) time without looping over all users, we would need 
    # to store this specific overlap. Since the current state doesn't have it,
    # we can store it in another sparse dict, or we can just compute the correction 
    # during _compute_auxiliary_data. Let's assume we update _compute_auxiliary_data
    # to include this overlap subtraction directly in `extra`.
    return profit


def x__compute_swap_profit__mutmut_1(
    instance: UFLPInstance,
    state: SolutionState,
    f_i: int,
    f_r: int,
) -> float:
    """
    Compute the EXACT profit of swapping f_r (open) with f_i (closed) in O(1).
    We use the precomputed save, loss, and extra arrays, but adjust for the
    double-counting of customers currently assigned to f_r who also benefit from f_i.
    """
    # If there's only 1 open facility, we can't use loss (it assumes phi2 is a valid backup)
    if len(state.open_facilities) != 1:
        d = instance.service_costs
        delta = instance.setup_costs[f_i] - instance.setup_costs[f_r]
        for u in instance.customers:
            delta += d[u][f_i] - d[u][f_r]
        return -delta

    profit = state.save[f_i] - state.loss[f_r] + state.extra.get((f_i, f_r), 0.0)
    
    # Correct the double-counting: for any customer where phi1 == f_r,
    # save[f_i] already included max(0, d(u, f_r) - d(u, f_i)).
    # We must subtract this out because the f_r facility is being removed,
    # and their true reassignment is handled by loss + extra.
    # To do this in O(1) time without looping over all users, we would need 
    # to store this specific overlap. Since the current state doesn't have it,
    # we can store it in another sparse dict, or we can just compute the correction 
    # during _compute_auxiliary_data. Let's assume we update _compute_auxiliary_data
    # to include this overlap subtraction directly in `extra`.
    return profit


def x__compute_swap_profit__mutmut_2(
    instance: UFLPInstance,
    state: SolutionState,
    f_i: int,
    f_r: int,
) -> float:
    """
    Compute the EXACT profit of swapping f_r (open) with f_i (closed) in O(1).
    We use the precomputed save, loss, and extra arrays, but adjust for the
    double-counting of customers currently assigned to f_r who also benefit from f_i.
    """
    # If there's only 1 open facility, we can't use loss (it assumes phi2 is a valid backup)
    if len(state.open_facilities) == 2:
        d = instance.service_costs
        delta = instance.setup_costs[f_i] - instance.setup_costs[f_r]
        for u in instance.customers:
            delta += d[u][f_i] - d[u][f_r]
        return -delta

    profit = state.save[f_i] - state.loss[f_r] + state.extra.get((f_i, f_r), 0.0)
    
    # Correct the double-counting: for any customer where phi1 == f_r,
    # save[f_i] already included max(0, d(u, f_r) - d(u, f_i)).
    # We must subtract this out because the f_r facility is being removed,
    # and their true reassignment is handled by loss + extra.
    # To do this in O(1) time without looping over all users, we would need 
    # to store this specific overlap. Since the current state doesn't have it,
    # we can store it in another sparse dict, or we can just compute the correction 
    # during _compute_auxiliary_data. Let's assume we update _compute_auxiliary_data
    # to include this overlap subtraction directly in `extra`.
    return profit


def x__compute_swap_profit__mutmut_3(
    instance: UFLPInstance,
    state: SolutionState,
    f_i: int,
    f_r: int,
) -> float:
    """
    Compute the EXACT profit of swapping f_r (open) with f_i (closed) in O(1).
    We use the precomputed save, loss, and extra arrays, but adjust for the
    double-counting of customers currently assigned to f_r who also benefit from f_i.
    """
    # If there's only 1 open facility, we can't use loss (it assumes phi2 is a valid backup)
    if len(state.open_facilities) == 1:
        d = None
        delta = instance.setup_costs[f_i] - instance.setup_costs[f_r]
        for u in instance.customers:
            delta += d[u][f_i] - d[u][f_r]
        return -delta

    profit = state.save[f_i] - state.loss[f_r] + state.extra.get((f_i, f_r), 0.0)
    
    # Correct the double-counting: for any customer where phi1 == f_r,
    # save[f_i] already included max(0, d(u, f_r) - d(u, f_i)).
    # We must subtract this out because the f_r facility is being removed,
    # and their true reassignment is handled by loss + extra.
    # To do this in O(1) time without looping over all users, we would need 
    # to store this specific overlap. Since the current state doesn't have it,
    # we can store it in another sparse dict, or we can just compute the correction 
    # during _compute_auxiliary_data. Let's assume we update _compute_auxiliary_data
    # to include this overlap subtraction directly in `extra`.
    return profit


def x__compute_swap_profit__mutmut_4(
    instance: UFLPInstance,
    state: SolutionState,
    f_i: int,
    f_r: int,
) -> float:
    """
    Compute the EXACT profit of swapping f_r (open) with f_i (closed) in O(1).
    We use the precomputed save, loss, and extra arrays, but adjust for the
    double-counting of customers currently assigned to f_r who also benefit from f_i.
    """
    # If there's only 1 open facility, we can't use loss (it assumes phi2 is a valid backup)
    if len(state.open_facilities) == 1:
        d = instance.service_costs
        delta = None
        for u in instance.customers:
            delta += d[u][f_i] - d[u][f_r]
        return -delta

    profit = state.save[f_i] - state.loss[f_r] + state.extra.get((f_i, f_r), 0.0)
    
    # Correct the double-counting: for any customer where phi1 == f_r,
    # save[f_i] already included max(0, d(u, f_r) - d(u, f_i)).
    # We must subtract this out because the f_r facility is being removed,
    # and their true reassignment is handled by loss + extra.
    # To do this in O(1) time without looping over all users, we would need 
    # to store this specific overlap. Since the current state doesn't have it,
    # we can store it in another sparse dict, or we can just compute the correction 
    # during _compute_auxiliary_data. Let's assume we update _compute_auxiliary_data
    # to include this overlap subtraction directly in `extra`.
    return profit


def x__compute_swap_profit__mutmut_5(
    instance: UFLPInstance,
    state: SolutionState,
    f_i: int,
    f_r: int,
) -> float:
    """
    Compute the EXACT profit of swapping f_r (open) with f_i (closed) in O(1).
    We use the precomputed save, loss, and extra arrays, but adjust for the
    double-counting of customers currently assigned to f_r who also benefit from f_i.
    """
    # If there's only 1 open facility, we can't use loss (it assumes phi2 is a valid backup)
    if len(state.open_facilities) == 1:
        d = instance.service_costs
        delta = instance.setup_costs[f_i] + instance.setup_costs[f_r]
        for u in instance.customers:
            delta += d[u][f_i] - d[u][f_r]
        return -delta

    profit = state.save[f_i] - state.loss[f_r] + state.extra.get((f_i, f_r), 0.0)
    
    # Correct the double-counting: for any customer where phi1 == f_r,
    # save[f_i] already included max(0, d(u, f_r) - d(u, f_i)).
    # We must subtract this out because the f_r facility is being removed,
    # and their true reassignment is handled by loss + extra.
    # To do this in O(1) time without looping over all users, we would need 
    # to store this specific overlap. Since the current state doesn't have it,
    # we can store it in another sparse dict, or we can just compute the correction 
    # during _compute_auxiliary_data. Let's assume we update _compute_auxiliary_data
    # to include this overlap subtraction directly in `extra`.
    return profit


def x__compute_swap_profit__mutmut_6(
    instance: UFLPInstance,
    state: SolutionState,
    f_i: int,
    f_r: int,
) -> float:
    """
    Compute the EXACT profit of swapping f_r (open) with f_i (closed) in O(1).
    We use the precomputed save, loss, and extra arrays, but adjust for the
    double-counting of customers currently assigned to f_r who also benefit from f_i.
    """
    # If there's only 1 open facility, we can't use loss (it assumes phi2 is a valid backup)
    if len(state.open_facilities) == 1:
        d = instance.service_costs
        delta = instance.setup_costs[f_i] - instance.setup_costs[f_r]
        for u in instance.customers:
            delta = d[u][f_i] - d[u][f_r]
        return -delta

    profit = state.save[f_i] - state.loss[f_r] + state.extra.get((f_i, f_r), 0.0)
    
    # Correct the double-counting: for any customer where phi1 == f_r,
    # save[f_i] already included max(0, d(u, f_r) - d(u, f_i)).
    # We must subtract this out because the f_r facility is being removed,
    # and their true reassignment is handled by loss + extra.
    # To do this in O(1) time without looping over all users, we would need 
    # to store this specific overlap. Since the current state doesn't have it,
    # we can store it in another sparse dict, or we can just compute the correction 
    # during _compute_auxiliary_data. Let's assume we update _compute_auxiliary_data
    # to include this overlap subtraction directly in `extra`.
    return profit


def x__compute_swap_profit__mutmut_7(
    instance: UFLPInstance,
    state: SolutionState,
    f_i: int,
    f_r: int,
) -> float:
    """
    Compute the EXACT profit of swapping f_r (open) with f_i (closed) in O(1).
    We use the precomputed save, loss, and extra arrays, but adjust for the
    double-counting of customers currently assigned to f_r who also benefit from f_i.
    """
    # If there's only 1 open facility, we can't use loss (it assumes phi2 is a valid backup)
    if len(state.open_facilities) == 1:
        d = instance.service_costs
        delta = instance.setup_costs[f_i] - instance.setup_costs[f_r]
        for u in instance.customers:
            delta -= d[u][f_i] - d[u][f_r]
        return -delta

    profit = state.save[f_i] - state.loss[f_r] + state.extra.get((f_i, f_r), 0.0)
    
    # Correct the double-counting: for any customer where phi1 == f_r,
    # save[f_i] already included max(0, d(u, f_r) - d(u, f_i)).
    # We must subtract this out because the f_r facility is being removed,
    # and their true reassignment is handled by loss + extra.
    # To do this in O(1) time without looping over all users, we would need 
    # to store this specific overlap. Since the current state doesn't have it,
    # we can store it in another sparse dict, or we can just compute the correction 
    # during _compute_auxiliary_data. Let's assume we update _compute_auxiliary_data
    # to include this overlap subtraction directly in `extra`.
    return profit


def x__compute_swap_profit__mutmut_8(
    instance: UFLPInstance,
    state: SolutionState,
    f_i: int,
    f_r: int,
) -> float:
    """
    Compute the EXACT profit of swapping f_r (open) with f_i (closed) in O(1).
    We use the precomputed save, loss, and extra arrays, but adjust for the
    double-counting of customers currently assigned to f_r who also benefit from f_i.
    """
    # If there's only 1 open facility, we can't use loss (it assumes phi2 is a valid backup)
    if len(state.open_facilities) == 1:
        d = instance.service_costs
        delta = instance.setup_costs[f_i] - instance.setup_costs[f_r]
        for u in instance.customers:
            delta += d[u][f_i] + d[u][f_r]
        return -delta

    profit = state.save[f_i] - state.loss[f_r] + state.extra.get((f_i, f_r), 0.0)
    
    # Correct the double-counting: for any customer where phi1 == f_r,
    # save[f_i] already included max(0, d(u, f_r) - d(u, f_i)).
    # We must subtract this out because the f_r facility is being removed,
    # and their true reassignment is handled by loss + extra.
    # To do this in O(1) time without looping over all users, we would need 
    # to store this specific overlap. Since the current state doesn't have it,
    # we can store it in another sparse dict, or we can just compute the correction 
    # during _compute_auxiliary_data. Let's assume we update _compute_auxiliary_data
    # to include this overlap subtraction directly in `extra`.
    return profit


def x__compute_swap_profit__mutmut_9(
    instance: UFLPInstance,
    state: SolutionState,
    f_i: int,
    f_r: int,
) -> float:
    """
    Compute the EXACT profit of swapping f_r (open) with f_i (closed) in O(1).
    We use the precomputed save, loss, and extra arrays, but adjust for the
    double-counting of customers currently assigned to f_r who also benefit from f_i.
    """
    # If there's only 1 open facility, we can't use loss (it assumes phi2 is a valid backup)
    if len(state.open_facilities) == 1:
        d = instance.service_costs
        delta = instance.setup_costs[f_i] - instance.setup_costs[f_r]
        for u in instance.customers:
            delta += d[u][f_i] - d[u][f_r]
        return +delta

    profit = state.save[f_i] - state.loss[f_r] + state.extra.get((f_i, f_r), 0.0)
    
    # Correct the double-counting: for any customer where phi1 == f_r,
    # save[f_i] already included max(0, d(u, f_r) - d(u, f_i)).
    # We must subtract this out because the f_r facility is being removed,
    # and their true reassignment is handled by loss + extra.
    # To do this in O(1) time without looping over all users, we would need 
    # to store this specific overlap. Since the current state doesn't have it,
    # we can store it in another sparse dict, or we can just compute the correction 
    # during _compute_auxiliary_data. Let's assume we update _compute_auxiliary_data
    # to include this overlap subtraction directly in `extra`.
    return profit


def x__compute_swap_profit__mutmut_10(
    instance: UFLPInstance,
    state: SolutionState,
    f_i: int,
    f_r: int,
) -> float:
    """
    Compute the EXACT profit of swapping f_r (open) with f_i (closed) in O(1).
    We use the precomputed save, loss, and extra arrays, but adjust for the
    double-counting of customers currently assigned to f_r who also benefit from f_i.
    """
    # If there's only 1 open facility, we can't use loss (it assumes phi2 is a valid backup)
    if len(state.open_facilities) == 1:
        d = instance.service_costs
        delta = instance.setup_costs[f_i] - instance.setup_costs[f_r]
        for u in instance.customers:
            delta += d[u][f_i] - d[u][f_r]
        return -delta

    profit = None
    
    # Correct the double-counting: for any customer where phi1 == f_r,
    # save[f_i] already included max(0, d(u, f_r) - d(u, f_i)).
    # We must subtract this out because the f_r facility is being removed,
    # and their true reassignment is handled by loss + extra.
    # To do this in O(1) time without looping over all users, we would need 
    # to store this specific overlap. Since the current state doesn't have it,
    # we can store it in another sparse dict, or we can just compute the correction 
    # during _compute_auxiliary_data. Let's assume we update _compute_auxiliary_data
    # to include this overlap subtraction directly in `extra`.
    return profit


def x__compute_swap_profit__mutmut_11(
    instance: UFLPInstance,
    state: SolutionState,
    f_i: int,
    f_r: int,
) -> float:
    """
    Compute the EXACT profit of swapping f_r (open) with f_i (closed) in O(1).
    We use the precomputed save, loss, and extra arrays, but adjust for the
    double-counting of customers currently assigned to f_r who also benefit from f_i.
    """
    # If there's only 1 open facility, we can't use loss (it assumes phi2 is a valid backup)
    if len(state.open_facilities) == 1:
        d = instance.service_costs
        delta = instance.setup_costs[f_i] - instance.setup_costs[f_r]
        for u in instance.customers:
            delta += d[u][f_i] - d[u][f_r]
        return -delta

    profit = state.save[f_i] - state.loss[f_r] - state.extra.get((f_i, f_r), 0.0)
    
    # Correct the double-counting: for any customer where phi1 == f_r,
    # save[f_i] already included max(0, d(u, f_r) - d(u, f_i)).
    # We must subtract this out because the f_r facility is being removed,
    # and their true reassignment is handled by loss + extra.
    # To do this in O(1) time without looping over all users, we would need 
    # to store this specific overlap. Since the current state doesn't have it,
    # we can store it in another sparse dict, or we can just compute the correction 
    # during _compute_auxiliary_data. Let's assume we update _compute_auxiliary_data
    # to include this overlap subtraction directly in `extra`.
    return profit


def x__compute_swap_profit__mutmut_12(
    instance: UFLPInstance,
    state: SolutionState,
    f_i: int,
    f_r: int,
) -> float:
    """
    Compute the EXACT profit of swapping f_r (open) with f_i (closed) in O(1).
    We use the precomputed save, loss, and extra arrays, but adjust for the
    double-counting of customers currently assigned to f_r who also benefit from f_i.
    """
    # If there's only 1 open facility, we can't use loss (it assumes phi2 is a valid backup)
    if len(state.open_facilities) == 1:
        d = instance.service_costs
        delta = instance.setup_costs[f_i] - instance.setup_costs[f_r]
        for u in instance.customers:
            delta += d[u][f_i] - d[u][f_r]
        return -delta

    profit = state.save[f_i] + state.loss[f_r] + state.extra.get((f_i, f_r), 0.0)
    
    # Correct the double-counting: for any customer where phi1 == f_r,
    # save[f_i] already included max(0, d(u, f_r) - d(u, f_i)).
    # We must subtract this out because the f_r facility is being removed,
    # and their true reassignment is handled by loss + extra.
    # To do this in O(1) time without looping over all users, we would need 
    # to store this specific overlap. Since the current state doesn't have it,
    # we can store it in another sparse dict, or we can just compute the correction 
    # during _compute_auxiliary_data. Let's assume we update _compute_auxiliary_data
    # to include this overlap subtraction directly in `extra`.
    return profit


def x__compute_swap_profit__mutmut_13(
    instance: UFLPInstance,
    state: SolutionState,
    f_i: int,
    f_r: int,
) -> float:
    """
    Compute the EXACT profit of swapping f_r (open) with f_i (closed) in O(1).
    We use the precomputed save, loss, and extra arrays, but adjust for the
    double-counting of customers currently assigned to f_r who also benefit from f_i.
    """
    # If there's only 1 open facility, we can't use loss (it assumes phi2 is a valid backup)
    if len(state.open_facilities) == 1:
        d = instance.service_costs
        delta = instance.setup_costs[f_i] - instance.setup_costs[f_r]
        for u in instance.customers:
            delta += d[u][f_i] - d[u][f_r]
        return -delta

    profit = state.save[f_i] - state.loss[f_r] + state.extra.get(None, 0.0)
    
    # Correct the double-counting: for any customer where phi1 == f_r,
    # save[f_i] already included max(0, d(u, f_r) - d(u, f_i)).
    # We must subtract this out because the f_r facility is being removed,
    # and their true reassignment is handled by loss + extra.
    # To do this in O(1) time without looping over all users, we would need 
    # to store this specific overlap. Since the current state doesn't have it,
    # we can store it in another sparse dict, or we can just compute the correction 
    # during _compute_auxiliary_data. Let's assume we update _compute_auxiliary_data
    # to include this overlap subtraction directly in `extra`.
    return profit


def x__compute_swap_profit__mutmut_14(
    instance: UFLPInstance,
    state: SolutionState,
    f_i: int,
    f_r: int,
) -> float:
    """
    Compute the EXACT profit of swapping f_r (open) with f_i (closed) in O(1).
    We use the precomputed save, loss, and extra arrays, but adjust for the
    double-counting of customers currently assigned to f_r who also benefit from f_i.
    """
    # If there's only 1 open facility, we can't use loss (it assumes phi2 is a valid backup)
    if len(state.open_facilities) == 1:
        d = instance.service_costs
        delta = instance.setup_costs[f_i] - instance.setup_costs[f_r]
        for u in instance.customers:
            delta += d[u][f_i] - d[u][f_r]
        return -delta

    profit = state.save[f_i] - state.loss[f_r] + state.extra.get((f_i, f_r), None)
    
    # Correct the double-counting: for any customer where phi1 == f_r,
    # save[f_i] already included max(0, d(u, f_r) - d(u, f_i)).
    # We must subtract this out because the f_r facility is being removed,
    # and their true reassignment is handled by loss + extra.
    # To do this in O(1) time without looping over all users, we would need 
    # to store this specific overlap. Since the current state doesn't have it,
    # we can store it in another sparse dict, or we can just compute the correction 
    # during _compute_auxiliary_data. Let's assume we update _compute_auxiliary_data
    # to include this overlap subtraction directly in `extra`.
    return profit


def x__compute_swap_profit__mutmut_15(
    instance: UFLPInstance,
    state: SolutionState,
    f_i: int,
    f_r: int,
) -> float:
    """
    Compute the EXACT profit of swapping f_r (open) with f_i (closed) in O(1).
    We use the precomputed save, loss, and extra arrays, but adjust for the
    double-counting of customers currently assigned to f_r who also benefit from f_i.
    """
    # If there's only 1 open facility, we can't use loss (it assumes phi2 is a valid backup)
    if len(state.open_facilities) == 1:
        d = instance.service_costs
        delta = instance.setup_costs[f_i] - instance.setup_costs[f_r]
        for u in instance.customers:
            delta += d[u][f_i] - d[u][f_r]
        return -delta

    profit = state.save[f_i] - state.loss[f_r] + state.extra.get(0.0)
    
    # Correct the double-counting: for any customer where phi1 == f_r,
    # save[f_i] already included max(0, d(u, f_r) - d(u, f_i)).
    # We must subtract this out because the f_r facility is being removed,
    # and their true reassignment is handled by loss + extra.
    # To do this in O(1) time without looping over all users, we would need 
    # to store this specific overlap. Since the current state doesn't have it,
    # we can store it in another sparse dict, or we can just compute the correction 
    # during _compute_auxiliary_data. Let's assume we update _compute_auxiliary_data
    # to include this overlap subtraction directly in `extra`.
    return profit


def x__compute_swap_profit__mutmut_16(
    instance: UFLPInstance,
    state: SolutionState,
    f_i: int,
    f_r: int,
) -> float:
    """
    Compute the EXACT profit of swapping f_r (open) with f_i (closed) in O(1).
    We use the precomputed save, loss, and extra arrays, but adjust for the
    double-counting of customers currently assigned to f_r who also benefit from f_i.
    """
    # If there's only 1 open facility, we can't use loss (it assumes phi2 is a valid backup)
    if len(state.open_facilities) == 1:
        d = instance.service_costs
        delta = instance.setup_costs[f_i] - instance.setup_costs[f_r]
        for u in instance.customers:
            delta += d[u][f_i] - d[u][f_r]
        return -delta

    profit = state.save[f_i] - state.loss[f_r] + state.extra.get((f_i, f_r), )
    
    # Correct the double-counting: for any customer where phi1 == f_r,
    # save[f_i] already included max(0, d(u, f_r) - d(u, f_i)).
    # We must subtract this out because the f_r facility is being removed,
    # and their true reassignment is handled by loss + extra.
    # To do this in O(1) time without looping over all users, we would need 
    # to store this specific overlap. Since the current state doesn't have it,
    # we can store it in another sparse dict, or we can just compute the correction 
    # during _compute_auxiliary_data. Let's assume we update _compute_auxiliary_data
    # to include this overlap subtraction directly in `extra`.
    return profit


def x__compute_swap_profit__mutmut_17(
    instance: UFLPInstance,
    state: SolutionState,
    f_i: int,
    f_r: int,
) -> float:
    """
    Compute the EXACT profit of swapping f_r (open) with f_i (closed) in O(1).
    We use the precomputed save, loss, and extra arrays, but adjust for the
    double-counting of customers currently assigned to f_r who also benefit from f_i.
    """
    # If there's only 1 open facility, we can't use loss (it assumes phi2 is a valid backup)
    if len(state.open_facilities) == 1:
        d = instance.service_costs
        delta = instance.setup_costs[f_i] - instance.setup_costs[f_r]
        for u in instance.customers:
            delta += d[u][f_i] - d[u][f_r]
        return -delta

    profit = state.save[f_i] - state.loss[f_r] + state.extra.get((f_i, f_r), 1.0)
    
    # Correct the double-counting: for any customer where phi1 == f_r,
    # save[f_i] already included max(0, d(u, f_r) - d(u, f_i)).
    # We must subtract this out because the f_r facility is being removed,
    # and their true reassignment is handled by loss + extra.
    # To do this in O(1) time without looping over all users, we would need 
    # to store this specific overlap. Since the current state doesn't have it,
    # we can store it in another sparse dict, or we can just compute the correction 
    # during _compute_auxiliary_data. Let's assume we update _compute_auxiliary_data
    # to include this overlap subtraction directly in `extra`.
    return profit

mutants_x__compute_swap_profit__mutmut['_mutmut_orig'] = x__compute_swap_profit__mutmut_orig # type: ignore # mutmut generated
mutants_x__compute_swap_profit__mutmut['x__compute_swap_profit__mutmut_1'] = x__compute_swap_profit__mutmut_1 # type: ignore # mutmut generated
mutants_x__compute_swap_profit__mutmut['x__compute_swap_profit__mutmut_2'] = x__compute_swap_profit__mutmut_2 # type: ignore # mutmut generated
mutants_x__compute_swap_profit__mutmut['x__compute_swap_profit__mutmut_3'] = x__compute_swap_profit__mutmut_3 # type: ignore # mutmut generated
mutants_x__compute_swap_profit__mutmut['x__compute_swap_profit__mutmut_4'] = x__compute_swap_profit__mutmut_4 # type: ignore # mutmut generated
mutants_x__compute_swap_profit__mutmut['x__compute_swap_profit__mutmut_5'] = x__compute_swap_profit__mutmut_5 # type: ignore # mutmut generated
mutants_x__compute_swap_profit__mutmut['x__compute_swap_profit__mutmut_6'] = x__compute_swap_profit__mutmut_6 # type: ignore # mutmut generated
mutants_x__compute_swap_profit__mutmut['x__compute_swap_profit__mutmut_7'] = x__compute_swap_profit__mutmut_7 # type: ignore # mutmut generated
mutants_x__compute_swap_profit__mutmut['x__compute_swap_profit__mutmut_8'] = x__compute_swap_profit__mutmut_8 # type: ignore # mutmut generated
mutants_x__compute_swap_profit__mutmut['x__compute_swap_profit__mutmut_9'] = x__compute_swap_profit__mutmut_9 # type: ignore # mutmut generated
mutants_x__compute_swap_profit__mutmut['x__compute_swap_profit__mutmut_10'] = x__compute_swap_profit__mutmut_10 # type: ignore # mutmut generated
mutants_x__compute_swap_profit__mutmut['x__compute_swap_profit__mutmut_11'] = x__compute_swap_profit__mutmut_11 # type: ignore # mutmut generated
mutants_x__compute_swap_profit__mutmut['x__compute_swap_profit__mutmut_12'] = x__compute_swap_profit__mutmut_12 # type: ignore # mutmut generated
mutants_x__compute_swap_profit__mutmut['x__compute_swap_profit__mutmut_13'] = x__compute_swap_profit__mutmut_13 # type: ignore # mutmut generated
mutants_x__compute_swap_profit__mutmut['x__compute_swap_profit__mutmut_14'] = x__compute_swap_profit__mutmut_14 # type: ignore # mutmut generated
mutants_x__compute_swap_profit__mutmut['x__compute_swap_profit__mutmut_15'] = x__compute_swap_profit__mutmut_15 # type: ignore # mutmut generated
mutants_x__compute_swap_profit__mutmut['x__compute_swap_profit__mutmut_16'] = x__compute_swap_profit__mutmut_16 # type: ignore # mutmut generated
mutants_x__compute_swap_profit__mutmut['x__compute_swap_profit__mutmut_17'] = x__compute_swap_profit__mutmut_17 # type: ignore # mutmut generated


def _compute_insert_profit(
    instance: UFLPInstance,
    state: SolutionState,
    f_i: int,
) -> float:
    """Compute exact profit of inserting closed facility f_i in O(1)."""
    return state.save[f_i]
mutants_x__compute_delete_profit__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__compute_delete_profit__mutmut)
def _compute_delete_profit(
    instance: UFLPInstance,
    state: SolutionState,
    f_r: int,
) -> float:
    """Compute exact profit of removing open facility f_r in O(1)."""
    return -state.loss[f_r]


def x__compute_delete_profit__mutmut_orig(
    instance: UFLPInstance,
    state: SolutionState,
    f_r: int,
) -> float:
    """Compute exact profit of removing open facility f_r in O(1)."""
    return -state.loss[f_r]


def x__compute_delete_profit__mutmut_1(
    instance: UFLPInstance,
    state: SolutionState,
    f_r: int,
) -> float:
    """Compute exact profit of removing open facility f_r in O(1)."""
    return +state.loss[f_r]

mutants_x__compute_delete_profit__mutmut['_mutmut_orig'] = x__compute_delete_profit__mutmut_orig # type: ignore # mutmut generated
mutants_x__compute_delete_profit__mutmut['x__compute_delete_profit__mutmut_1'] = x__compute_delete_profit__mutmut_1 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_construct_solution__mutmut)
def construct_solution(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_orig(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_1(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = False,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_2(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = None
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_3(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 1.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_4(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = None

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_5(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = None
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_6(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(None, EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_7(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), None)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_8(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_9(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), )
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_10(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(None, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_11(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, None), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_12(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_13(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, ), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_14(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 1.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_15(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() <= prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_16(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(None)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_17(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_18(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = None
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_19(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            None,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_20(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=None,
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_21(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_22(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_23(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: None,
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_24(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f] - sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_25(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(None)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_26(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(None)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_27(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = None
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_28(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            None, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_29(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, None, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_30(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, None
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_31(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_32(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_33(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_34(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = None
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_35(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = None

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_36(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = None
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_37(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(None, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_38(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, None)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_39(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_40(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, )
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_41(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(None, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_42(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, None)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_43(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_44(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, )

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_45(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(None)
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_46(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(None)
        print(f"          Initial cost: {state.total_cost:,.2f}")

    return state


def x_construct_solution__mutmut_47(
    instance: UFLPInstance,
    lp_probs: Dict[int, float],
    verbose: bool = True,
) -> SolutionState:
    """
    Build an initial solution using LP-biased probabilistic construction.

    Each facility f is opened with probability max(lp_probs[f], eps) where eps=0.01.
    If the result is empty, we fall back to the single cheapest facility.
    """
    EPS = 0.01
    state = SolutionState()

    # --- Select facilities probabilistically ---
    for f in instance.facilities:
        prob = max(lp_probs.get(f, 0.0), EPS)
        if random.random() < prob:
            state.open_facilities.add(f)

    # --- Fallback: if nothing opened, pick the cheapest total-cost facility ---
    if not state.open_facilities:
        best_f = min(
            instance.facilities,
            key=lambda f: (
                instance.setup_costs[f]
                + sum(instance.service_costs[u][f] for u in instance.customers)
            ),
        )
        state.open_facilities.add(best_f)

    # --- Assign customers to closest & second closest ---
    for u in instance.customers:
        closest, second = _find_closest_two(
            u, state.open_facilities, instance.service_costs
        )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # --- Compute cost and auxiliary data ---
    state.total_cost = _compute_total_cost(instance, state)
    _compute_auxiliary_data(instance, state)

    if verbose:
        print(f"[Phase 2] Constructed initial solution")
        print(f"          Open facilities: {len(state.open_facilities)}/{len(instance.facilities)}")
        print(None)

    return state

mutants_x_construct_solution__mutmut['_mutmut_orig'] = x_construct_solution__mutmut_orig # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_1'] = x_construct_solution__mutmut_1 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_2'] = x_construct_solution__mutmut_2 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_3'] = x_construct_solution__mutmut_3 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_4'] = x_construct_solution__mutmut_4 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_5'] = x_construct_solution__mutmut_5 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_6'] = x_construct_solution__mutmut_6 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_7'] = x_construct_solution__mutmut_7 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_8'] = x_construct_solution__mutmut_8 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_9'] = x_construct_solution__mutmut_9 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_10'] = x_construct_solution__mutmut_10 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_11'] = x_construct_solution__mutmut_11 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_12'] = x_construct_solution__mutmut_12 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_13'] = x_construct_solution__mutmut_13 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_14'] = x_construct_solution__mutmut_14 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_15'] = x_construct_solution__mutmut_15 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_16'] = x_construct_solution__mutmut_16 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_17'] = x_construct_solution__mutmut_17 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_18'] = x_construct_solution__mutmut_18 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_19'] = x_construct_solution__mutmut_19 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_20'] = x_construct_solution__mutmut_20 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_21'] = x_construct_solution__mutmut_21 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_22'] = x_construct_solution__mutmut_22 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_23'] = x_construct_solution__mutmut_23 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_24'] = x_construct_solution__mutmut_24 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_25'] = x_construct_solution__mutmut_25 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_26'] = x_construct_solution__mutmut_26 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_27'] = x_construct_solution__mutmut_27 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_28'] = x_construct_solution__mutmut_28 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_29'] = x_construct_solution__mutmut_29 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_30'] = x_construct_solution__mutmut_30 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_31'] = x_construct_solution__mutmut_31 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_32'] = x_construct_solution__mutmut_32 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_33'] = x_construct_solution__mutmut_33 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_34'] = x_construct_solution__mutmut_34 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_35'] = x_construct_solution__mutmut_35 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_36'] = x_construct_solution__mutmut_36 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_37'] = x_construct_solution__mutmut_37 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_38'] = x_construct_solution__mutmut_38 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_39'] = x_construct_solution__mutmut_39 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_40'] = x_construct_solution__mutmut_40 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_41'] = x_construct_solution__mutmut_41 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_42'] = x_construct_solution__mutmut_42 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_43'] = x_construct_solution__mutmut_43 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_44'] = x_construct_solution__mutmut_44 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_45'] = x_construct_solution__mutmut_45 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_46'] = x_construct_solution__mutmut_46 # type: ignore # mutmut generated
mutants_x_construct_solution__mutmut['x_construct_solution__mutmut_47'] = x_construct_solution__mutmut_47 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut: MutantDict = {}  # type: ignore


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

@_mutmut_mutated(mutants_x__apply_move_and_recompute__mutmut)
def _apply_move_and_recompute(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_orig(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_1(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = None

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_2(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_3(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_4(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_5(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'XXinsertXX':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_6(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'INSERT':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_7(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(None)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_8(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'XXdeleteXX':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_9(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'DELETE':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_10(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(None)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_11(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'XXswapXX':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_12(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'SWAP':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_13(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(None)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_14(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(None)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_15(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = None
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_16(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(None, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_17(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, None, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_18(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, None)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_19(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_20(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_21(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, )
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_22(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = None
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_23(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = None

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_24(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = None

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_25(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(None, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_26(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, None)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_27(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_28(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, )

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_29(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(None, state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_30(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, None)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_31(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(state)


# ============================================================================
# Section 5: Phase 3 — Fast Local Search (Best-Improvement)
# ============================================================================

def x__apply_move_and_recompute__mutmut_32(
    instance: UFLPInstance,
    state: SolutionState,
    move_type: str,
    f_in: int | None,
    f_out: int | None,
) -> None:
    """
    Apply a move (insert/delete/swap) and fully recompute the solution state.
    """
    d = instance.service_costs

    match move_type:
        case 'insert':
            state.open_facilities.add(f_in)
        case 'delete':
            state.open_facilities.discard(f_out)
        case 'swap':
            state.open_facilities.add(f_in)
            state.open_facilities.discard(f_out)

    # Recompute closest/second_closest for all customers
    for u in instance.customers:
        closest, second = _find_closest_two(u, state.open_facilities, d)
        state.closest_facility[u] = closest
        state.second_closest_facility[u] = second

    # Recompute total cost
    state.total_cost = _compute_total_cost(instance, state)

    # Recompute auxiliary data
    _compute_auxiliary_data(instance, )

mutants_x__apply_move_and_recompute__mutmut['_mutmut_orig'] = x__apply_move_and_recompute__mutmut_orig # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_1'] = x__apply_move_and_recompute__mutmut_1 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_2'] = x__apply_move_and_recompute__mutmut_2 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_3'] = x__apply_move_and_recompute__mutmut_3 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_4'] = x__apply_move_and_recompute__mutmut_4 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_5'] = x__apply_move_and_recompute__mutmut_5 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_6'] = x__apply_move_and_recompute__mutmut_6 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_7'] = x__apply_move_and_recompute__mutmut_7 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_8'] = x__apply_move_and_recompute__mutmut_8 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_9'] = x__apply_move_and_recompute__mutmut_9 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_10'] = x__apply_move_and_recompute__mutmut_10 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_11'] = x__apply_move_and_recompute__mutmut_11 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_12'] = x__apply_move_and_recompute__mutmut_12 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_13'] = x__apply_move_and_recompute__mutmut_13 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_14'] = x__apply_move_and_recompute__mutmut_14 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_15'] = x__apply_move_and_recompute__mutmut_15 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_16'] = x__apply_move_and_recompute__mutmut_16 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_17'] = x__apply_move_and_recompute__mutmut_17 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_18'] = x__apply_move_and_recompute__mutmut_18 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_19'] = x__apply_move_and_recompute__mutmut_19 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_20'] = x__apply_move_and_recompute__mutmut_20 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_21'] = x__apply_move_and_recompute__mutmut_21 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_22'] = x__apply_move_and_recompute__mutmut_22 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_23'] = x__apply_move_and_recompute__mutmut_23 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_24'] = x__apply_move_and_recompute__mutmut_24 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_25'] = x__apply_move_and_recompute__mutmut_25 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_26'] = x__apply_move_and_recompute__mutmut_26 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_27'] = x__apply_move_and_recompute__mutmut_27 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_28'] = x__apply_move_and_recompute__mutmut_28 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_29'] = x__apply_move_and_recompute__mutmut_29 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_30'] = x__apply_move_and_recompute__mutmut_30 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_31'] = x__apply_move_and_recompute__mutmut_31 # type: ignore # mutmut generated
mutants_x__apply_move_and_recompute__mutmut['x__apply_move_and_recompute__mutmut_32'] = x__apply_move_and_recompute__mutmut_32 # type: ignore # mutmut generated
mutants_x_local_search__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_local_search__mutmut)
def local_search(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_orig(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_1(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = False,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_2(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = None

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_3(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 1

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_4(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while False:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_5(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = None  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_6(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1.000000001  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_7(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = ""

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_8(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = None

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_9(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_10(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = None
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_11(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(None, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_12(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, None, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_13(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, None)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_14(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_15(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_16(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, )
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_17(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit >= best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_18(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = None
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_19(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = None

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_20(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('XXinsertXX', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_21(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('INSERT', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_22(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) >= 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_23(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 2:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_24(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(None):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_25(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = None
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_26(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(None, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_27(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, None, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_28(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, None)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_29(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_30(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_31(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, )
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_32(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit >= best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_33(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = None
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_34(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = None

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_35(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('XXdeleteXX', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_36(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('DELETE', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_37(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(None):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_38(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = None
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_39(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(None, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_40(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, None, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_41(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, None, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_42(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, None)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_43(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_44(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_45(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_46(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, )
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_47(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit >= best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_48(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = None
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_49(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = None

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_50(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('XXswapXX', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_51(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('SWAP', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_52(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is not None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_53(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(None)
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_54(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            return

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_55(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = None
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_56(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration = 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_57(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration -= 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_58(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 2

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_59(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(None, state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_60(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, None, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_61(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, None, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_62(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, None, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_63(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, None)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_64(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(state, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_65(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, move_type, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_66(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, f_in, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_67(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_out)

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_68(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, )

        if verbose:
            print(
                f"          Iter {iteration:3d}: {move_type:6s} "
                f"(in={f_in}, out={f_out})  "
                f"profit={best_profit:+,.2f}  "
                f"cost={state.total_cost:,.2f}  "
                f"|S|={len(state.open_facilities)}"
            )

    return state


def x_local_search__mutmut_69(
    instance: UFLPInstance,
    state: SolutionState,
    verbose: bool = True,
) -> SolutionState:
    """
    Best-improvement local search over insertions, deletions, and swaps.

    Uses exact delta evaluation for each candidate move. At each iteration,
    evaluates all possible moves and applies the single move with the highest
    positive profit. Stops when no improving move exists (local optimum).

    The save/loss/extra structures are maintained in the SolutionState for
    external inspection, but move acceptance is based on exact cost deltas
    to prevent numerical cycling.
    """
    iteration = 0

    while True:
        best_profit = 1e-9  # minimum threshold to avoid numerical noise
        best_move: Tuple[str, int | None, int | None] | None = None

        closed_facs = [f for f in instance.facilities if f not in state.open_facilities]

        # --- 1. Evaluate insertions ---
        for f_i in closed_facs:
            profit = _compute_insert_profit(instance, state, f_i)
            if profit > best_profit:
                best_profit = profit
                best_move = ('insert', f_i, None)

        # --- 2. Evaluate deletions (only if >1 facility is open) ---
        if len(state.open_facilities) > 1:
            for f_r in list(state.open_facilities):
                profit = _compute_delete_profit(instance, state, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('delete', None, f_r)

        # --- 3. Evaluate swaps ---
        for f_i in closed_facs:
            for f_r in list(state.open_facilities):
                profit = _compute_swap_profit(instance, state, f_i, f_r)
                if profit > best_profit:
                    best_profit = profit
                    best_move = ('swap', f_i, f_r)

        # --- No improving move -> local optimum ---
        if best_move is None:
            if verbose:
                print(f"[Phase 3] Local search converged after {iteration} iterations")
            break

        # --- Apply the best move ---
        move_type, f_in, f_out = best_move
        iteration += 1

        _apply_move_and_recompute(instance, state, move_type, f_in, f_out)

        if verbose:
            print(
                None
            )

    return state

mutants_x_local_search__mutmut['_mutmut_orig'] = x_local_search__mutmut_orig # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_1'] = x_local_search__mutmut_1 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_2'] = x_local_search__mutmut_2 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_3'] = x_local_search__mutmut_3 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_4'] = x_local_search__mutmut_4 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_5'] = x_local_search__mutmut_5 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_6'] = x_local_search__mutmut_6 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_7'] = x_local_search__mutmut_7 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_8'] = x_local_search__mutmut_8 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_9'] = x_local_search__mutmut_9 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_10'] = x_local_search__mutmut_10 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_11'] = x_local_search__mutmut_11 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_12'] = x_local_search__mutmut_12 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_13'] = x_local_search__mutmut_13 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_14'] = x_local_search__mutmut_14 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_15'] = x_local_search__mutmut_15 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_16'] = x_local_search__mutmut_16 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_17'] = x_local_search__mutmut_17 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_18'] = x_local_search__mutmut_18 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_19'] = x_local_search__mutmut_19 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_20'] = x_local_search__mutmut_20 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_21'] = x_local_search__mutmut_21 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_22'] = x_local_search__mutmut_22 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_23'] = x_local_search__mutmut_23 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_24'] = x_local_search__mutmut_24 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_25'] = x_local_search__mutmut_25 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_26'] = x_local_search__mutmut_26 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_27'] = x_local_search__mutmut_27 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_28'] = x_local_search__mutmut_28 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_29'] = x_local_search__mutmut_29 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_30'] = x_local_search__mutmut_30 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_31'] = x_local_search__mutmut_31 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_32'] = x_local_search__mutmut_32 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_33'] = x_local_search__mutmut_33 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_34'] = x_local_search__mutmut_34 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_35'] = x_local_search__mutmut_35 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_36'] = x_local_search__mutmut_36 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_37'] = x_local_search__mutmut_37 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_38'] = x_local_search__mutmut_38 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_39'] = x_local_search__mutmut_39 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_40'] = x_local_search__mutmut_40 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_41'] = x_local_search__mutmut_41 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_42'] = x_local_search__mutmut_42 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_43'] = x_local_search__mutmut_43 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_44'] = x_local_search__mutmut_44 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_45'] = x_local_search__mutmut_45 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_46'] = x_local_search__mutmut_46 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_47'] = x_local_search__mutmut_47 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_48'] = x_local_search__mutmut_48 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_49'] = x_local_search__mutmut_49 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_50'] = x_local_search__mutmut_50 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_51'] = x_local_search__mutmut_51 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_52'] = x_local_search__mutmut_52 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_53'] = x_local_search__mutmut_53 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_54'] = x_local_search__mutmut_54 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_55'] = x_local_search__mutmut_55 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_56'] = x_local_search__mutmut_56 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_57'] = x_local_search__mutmut_57 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_58'] = x_local_search__mutmut_58 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_59'] = x_local_search__mutmut_59 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_60'] = x_local_search__mutmut_60 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_61'] = x_local_search__mutmut_61 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_62'] = x_local_search__mutmut_62 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_63'] = x_local_search__mutmut_63 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_64'] = x_local_search__mutmut_64 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_65'] = x_local_search__mutmut_65 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_66'] = x_local_search__mutmut_66 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_67'] = x_local_search__mutmut_67 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_68'] = x_local_search__mutmut_68 # type: ignore # mutmut generated
mutants_x_local_search__mutmut['x_local_search__mutmut_69'] = x_local_search__mutmut_69 # type: ignore # mutmut generated
mutants_x_validate_instance__mutmut: MutantDict = {}  # type: ignore


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

@_mutmut_mutated(mutants_x_validate_instance__mutmut)
def validate_instance(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if not instance.facilities:
        raise ValueError("UFLP instance must contain at least one facility")
    if not instance.customers:
        raise ValueError("UFLP instance must contain at least one customer")

    missing_setup = [f for f in instance.facilities if f not in instance.setup_costs]
    if missing_setup:
        raise ValueError(f"Missing setup costs for facilities: {missing_setup}")

    for u in instance.customers:
        row = instance.service_costs.get(u)
        if row is None:
            raise ValueError(f"Missing service costs for customer {u}")
        missing_service = [f for f in instance.facilities if f not in row]
        if missing_service:
            raise ValueError(
                f"Missing service costs for customer {u}, facilities: {missing_service}"
            )


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

def x_validate_instance__mutmut_orig(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if not instance.facilities:
        raise ValueError("UFLP instance must contain at least one facility")
    if not instance.customers:
        raise ValueError("UFLP instance must contain at least one customer")

    missing_setup = [f for f in instance.facilities if f not in instance.setup_costs]
    if missing_setup:
        raise ValueError(f"Missing setup costs for facilities: {missing_setup}")

    for u in instance.customers:
        row = instance.service_costs.get(u)
        if row is None:
            raise ValueError(f"Missing service costs for customer {u}")
        missing_service = [f for f in instance.facilities if f not in row]
        if missing_service:
            raise ValueError(
                f"Missing service costs for customer {u}, facilities: {missing_service}"
            )


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

def x_validate_instance__mutmut_1(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if instance.facilities:
        raise ValueError("UFLP instance must contain at least one facility")
    if not instance.customers:
        raise ValueError("UFLP instance must contain at least one customer")

    missing_setup = [f for f in instance.facilities if f not in instance.setup_costs]
    if missing_setup:
        raise ValueError(f"Missing setup costs for facilities: {missing_setup}")

    for u in instance.customers:
        row = instance.service_costs.get(u)
        if row is None:
            raise ValueError(f"Missing service costs for customer {u}")
        missing_service = [f for f in instance.facilities if f not in row]
        if missing_service:
            raise ValueError(
                f"Missing service costs for customer {u}, facilities: {missing_service}"
            )


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

def x_validate_instance__mutmut_2(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if not instance.facilities:
        raise ValueError(None)
    if not instance.customers:
        raise ValueError("UFLP instance must contain at least one customer")

    missing_setup = [f for f in instance.facilities if f not in instance.setup_costs]
    if missing_setup:
        raise ValueError(f"Missing setup costs for facilities: {missing_setup}")

    for u in instance.customers:
        row = instance.service_costs.get(u)
        if row is None:
            raise ValueError(f"Missing service costs for customer {u}")
        missing_service = [f for f in instance.facilities if f not in row]
        if missing_service:
            raise ValueError(
                f"Missing service costs for customer {u}, facilities: {missing_service}"
            )


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

def x_validate_instance__mutmut_3(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if not instance.facilities:
        raise ValueError("XXUFLP instance must contain at least one facilityXX")
    if not instance.customers:
        raise ValueError("UFLP instance must contain at least one customer")

    missing_setup = [f for f in instance.facilities if f not in instance.setup_costs]
    if missing_setup:
        raise ValueError(f"Missing setup costs for facilities: {missing_setup}")

    for u in instance.customers:
        row = instance.service_costs.get(u)
        if row is None:
            raise ValueError(f"Missing service costs for customer {u}")
        missing_service = [f for f in instance.facilities if f not in row]
        if missing_service:
            raise ValueError(
                f"Missing service costs for customer {u}, facilities: {missing_service}"
            )


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

def x_validate_instance__mutmut_4(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if not instance.facilities:
        raise ValueError("uflp instance must contain at least one facility")
    if not instance.customers:
        raise ValueError("UFLP instance must contain at least one customer")

    missing_setup = [f for f in instance.facilities if f not in instance.setup_costs]
    if missing_setup:
        raise ValueError(f"Missing setup costs for facilities: {missing_setup}")

    for u in instance.customers:
        row = instance.service_costs.get(u)
        if row is None:
            raise ValueError(f"Missing service costs for customer {u}")
        missing_service = [f for f in instance.facilities if f not in row]
        if missing_service:
            raise ValueError(
                f"Missing service costs for customer {u}, facilities: {missing_service}"
            )


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

def x_validate_instance__mutmut_5(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if not instance.facilities:
        raise ValueError("UFLP INSTANCE MUST CONTAIN AT LEAST ONE FACILITY")
    if not instance.customers:
        raise ValueError("UFLP instance must contain at least one customer")

    missing_setup = [f for f in instance.facilities if f not in instance.setup_costs]
    if missing_setup:
        raise ValueError(f"Missing setup costs for facilities: {missing_setup}")

    for u in instance.customers:
        row = instance.service_costs.get(u)
        if row is None:
            raise ValueError(f"Missing service costs for customer {u}")
        missing_service = [f for f in instance.facilities if f not in row]
        if missing_service:
            raise ValueError(
                f"Missing service costs for customer {u}, facilities: {missing_service}"
            )


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

def x_validate_instance__mutmut_6(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if not instance.facilities:
        raise ValueError("UFLP instance must contain at least one facility")
    if instance.customers:
        raise ValueError("UFLP instance must contain at least one customer")

    missing_setup = [f for f in instance.facilities if f not in instance.setup_costs]
    if missing_setup:
        raise ValueError(f"Missing setup costs for facilities: {missing_setup}")

    for u in instance.customers:
        row = instance.service_costs.get(u)
        if row is None:
            raise ValueError(f"Missing service costs for customer {u}")
        missing_service = [f for f in instance.facilities if f not in row]
        if missing_service:
            raise ValueError(
                f"Missing service costs for customer {u}, facilities: {missing_service}"
            )


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

def x_validate_instance__mutmut_7(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if not instance.facilities:
        raise ValueError("UFLP instance must contain at least one facility")
    if not instance.customers:
        raise ValueError(None)

    missing_setup = [f for f in instance.facilities if f not in instance.setup_costs]
    if missing_setup:
        raise ValueError(f"Missing setup costs for facilities: {missing_setup}")

    for u in instance.customers:
        row = instance.service_costs.get(u)
        if row is None:
            raise ValueError(f"Missing service costs for customer {u}")
        missing_service = [f for f in instance.facilities if f not in row]
        if missing_service:
            raise ValueError(
                f"Missing service costs for customer {u}, facilities: {missing_service}"
            )


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

def x_validate_instance__mutmut_8(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if not instance.facilities:
        raise ValueError("UFLP instance must contain at least one facility")
    if not instance.customers:
        raise ValueError("XXUFLP instance must contain at least one customerXX")

    missing_setup = [f for f in instance.facilities if f not in instance.setup_costs]
    if missing_setup:
        raise ValueError(f"Missing setup costs for facilities: {missing_setup}")

    for u in instance.customers:
        row = instance.service_costs.get(u)
        if row is None:
            raise ValueError(f"Missing service costs for customer {u}")
        missing_service = [f for f in instance.facilities if f not in row]
        if missing_service:
            raise ValueError(
                f"Missing service costs for customer {u}, facilities: {missing_service}"
            )


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

def x_validate_instance__mutmut_9(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if not instance.facilities:
        raise ValueError("UFLP instance must contain at least one facility")
    if not instance.customers:
        raise ValueError("uflp instance must contain at least one customer")

    missing_setup = [f for f in instance.facilities if f not in instance.setup_costs]
    if missing_setup:
        raise ValueError(f"Missing setup costs for facilities: {missing_setup}")

    for u in instance.customers:
        row = instance.service_costs.get(u)
        if row is None:
            raise ValueError(f"Missing service costs for customer {u}")
        missing_service = [f for f in instance.facilities if f not in row]
        if missing_service:
            raise ValueError(
                f"Missing service costs for customer {u}, facilities: {missing_service}"
            )


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

def x_validate_instance__mutmut_10(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if not instance.facilities:
        raise ValueError("UFLP instance must contain at least one facility")
    if not instance.customers:
        raise ValueError("UFLP INSTANCE MUST CONTAIN AT LEAST ONE CUSTOMER")

    missing_setup = [f for f in instance.facilities if f not in instance.setup_costs]
    if missing_setup:
        raise ValueError(f"Missing setup costs for facilities: {missing_setup}")

    for u in instance.customers:
        row = instance.service_costs.get(u)
        if row is None:
            raise ValueError(f"Missing service costs for customer {u}")
        missing_service = [f for f in instance.facilities if f not in row]
        if missing_service:
            raise ValueError(
                f"Missing service costs for customer {u}, facilities: {missing_service}"
            )


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

def x_validate_instance__mutmut_11(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if not instance.facilities:
        raise ValueError("UFLP instance must contain at least one facility")
    if not instance.customers:
        raise ValueError("UFLP instance must contain at least one customer")

    missing_setup = None
    if missing_setup:
        raise ValueError(f"Missing setup costs for facilities: {missing_setup}")

    for u in instance.customers:
        row = instance.service_costs.get(u)
        if row is None:
            raise ValueError(f"Missing service costs for customer {u}")
        missing_service = [f for f in instance.facilities if f not in row]
        if missing_service:
            raise ValueError(
                f"Missing service costs for customer {u}, facilities: {missing_service}"
            )


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

def x_validate_instance__mutmut_12(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if not instance.facilities:
        raise ValueError("UFLP instance must contain at least one facility")
    if not instance.customers:
        raise ValueError("UFLP instance must contain at least one customer")

    missing_setup = [f for f in instance.facilities if f in instance.setup_costs]
    if missing_setup:
        raise ValueError(f"Missing setup costs for facilities: {missing_setup}")

    for u in instance.customers:
        row = instance.service_costs.get(u)
        if row is None:
            raise ValueError(f"Missing service costs for customer {u}")
        missing_service = [f for f in instance.facilities if f not in row]
        if missing_service:
            raise ValueError(
                f"Missing service costs for customer {u}, facilities: {missing_service}"
            )


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

def x_validate_instance__mutmut_13(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if not instance.facilities:
        raise ValueError("UFLP instance must contain at least one facility")
    if not instance.customers:
        raise ValueError("UFLP instance must contain at least one customer")

    missing_setup = [f for f in instance.facilities if f not in instance.setup_costs]
    if missing_setup:
        raise ValueError(None)

    for u in instance.customers:
        row = instance.service_costs.get(u)
        if row is None:
            raise ValueError(f"Missing service costs for customer {u}")
        missing_service = [f for f in instance.facilities if f not in row]
        if missing_service:
            raise ValueError(
                f"Missing service costs for customer {u}, facilities: {missing_service}"
            )


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

def x_validate_instance__mutmut_14(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if not instance.facilities:
        raise ValueError("UFLP instance must contain at least one facility")
    if not instance.customers:
        raise ValueError("UFLP instance must contain at least one customer")

    missing_setup = [f for f in instance.facilities if f not in instance.setup_costs]
    if missing_setup:
        raise ValueError(f"Missing setup costs for facilities: {missing_setup}")

    for u in instance.customers:
        row = None
        if row is None:
            raise ValueError(f"Missing service costs for customer {u}")
        missing_service = [f for f in instance.facilities if f not in row]
        if missing_service:
            raise ValueError(
                f"Missing service costs for customer {u}, facilities: {missing_service}"
            )


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

def x_validate_instance__mutmut_15(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if not instance.facilities:
        raise ValueError("UFLP instance must contain at least one facility")
    if not instance.customers:
        raise ValueError("UFLP instance must contain at least one customer")

    missing_setup = [f for f in instance.facilities if f not in instance.setup_costs]
    if missing_setup:
        raise ValueError(f"Missing setup costs for facilities: {missing_setup}")

    for u in instance.customers:
        row = instance.service_costs.get(None)
        if row is None:
            raise ValueError(f"Missing service costs for customer {u}")
        missing_service = [f for f in instance.facilities if f not in row]
        if missing_service:
            raise ValueError(
                f"Missing service costs for customer {u}, facilities: {missing_service}"
            )


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

def x_validate_instance__mutmut_16(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if not instance.facilities:
        raise ValueError("UFLP instance must contain at least one facility")
    if not instance.customers:
        raise ValueError("UFLP instance must contain at least one customer")

    missing_setup = [f for f in instance.facilities if f not in instance.setup_costs]
    if missing_setup:
        raise ValueError(f"Missing setup costs for facilities: {missing_setup}")

    for u in instance.customers:
        row = instance.service_costs.get(u)
        if row is not None:
            raise ValueError(f"Missing service costs for customer {u}")
        missing_service = [f for f in instance.facilities if f not in row]
        if missing_service:
            raise ValueError(
                f"Missing service costs for customer {u}, facilities: {missing_service}"
            )


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

def x_validate_instance__mutmut_17(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if not instance.facilities:
        raise ValueError("UFLP instance must contain at least one facility")
    if not instance.customers:
        raise ValueError("UFLP instance must contain at least one customer")

    missing_setup = [f for f in instance.facilities if f not in instance.setup_costs]
    if missing_setup:
        raise ValueError(f"Missing setup costs for facilities: {missing_setup}")

    for u in instance.customers:
        row = instance.service_costs.get(u)
        if row is None:
            raise ValueError(None)
        missing_service = [f for f in instance.facilities if f not in row]
        if missing_service:
            raise ValueError(
                f"Missing service costs for customer {u}, facilities: {missing_service}"
            )


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

def x_validate_instance__mutmut_18(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if not instance.facilities:
        raise ValueError("UFLP instance must contain at least one facility")
    if not instance.customers:
        raise ValueError("UFLP instance must contain at least one customer")

    missing_setup = [f for f in instance.facilities if f not in instance.setup_costs]
    if missing_setup:
        raise ValueError(f"Missing setup costs for facilities: {missing_setup}")

    for u in instance.customers:
        row = instance.service_costs.get(u)
        if row is None:
            raise ValueError(f"Missing service costs for customer {u}")
        missing_service = None
        if missing_service:
            raise ValueError(
                f"Missing service costs for customer {u}, facilities: {missing_service}"
            )


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

def x_validate_instance__mutmut_19(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if not instance.facilities:
        raise ValueError("UFLP instance must contain at least one facility")
    if not instance.customers:
        raise ValueError("UFLP instance must contain at least one customer")

    missing_setup = [f for f in instance.facilities if f not in instance.setup_costs]
    if missing_setup:
        raise ValueError(f"Missing setup costs for facilities: {missing_setup}")

    for u in instance.customers:
        row = instance.service_costs.get(u)
        if row is None:
            raise ValueError(f"Missing service costs for customer {u}")
        missing_service = [f for f in instance.facilities if f in row]
        if missing_service:
            raise ValueError(
                f"Missing service costs for customer {u}, facilities: {missing_service}"
            )


# ============================================================================
# Section 6: Phase 4 — Main Orchestration
# ============================================================================

def x_validate_instance__mutmut_20(instance: UFLPInstance) -> None:
    """
    Reject degenerate or incomplete instances before any solving happens.

    Without this guard a customer-free instance silently returns a non-zero
    cost (the construction fallback force-opens one facility and the local
    search may never delete the last open facility), and a facility-free
    instance surfaces as an opaque SciPy error from deep inside `linprog`.
    """
    if not instance.facilities:
        raise ValueError("UFLP instance must contain at least one facility")
    if not instance.customers:
        raise ValueError("UFLP instance must contain at least one customer")

    missing_setup = [f for f in instance.facilities if f not in instance.setup_costs]
    if missing_setup:
        raise ValueError(f"Missing setup costs for facilities: {missing_setup}")

    for u in instance.customers:
        row = instance.service_costs.get(u)
        if row is None:
            raise ValueError(f"Missing service costs for customer {u}")
        missing_service = [f for f in instance.facilities if f not in row]
        if missing_service:
            raise ValueError(
                None
            )

mutants_x_validate_instance__mutmut['_mutmut_orig'] = x_validate_instance__mutmut_orig # type: ignore # mutmut generated
mutants_x_validate_instance__mutmut['x_validate_instance__mutmut_1'] = x_validate_instance__mutmut_1 # type: ignore # mutmut generated
mutants_x_validate_instance__mutmut['x_validate_instance__mutmut_2'] = x_validate_instance__mutmut_2 # type: ignore # mutmut generated
mutants_x_validate_instance__mutmut['x_validate_instance__mutmut_3'] = x_validate_instance__mutmut_3 # type: ignore # mutmut generated
mutants_x_validate_instance__mutmut['x_validate_instance__mutmut_4'] = x_validate_instance__mutmut_4 # type: ignore # mutmut generated
mutants_x_validate_instance__mutmut['x_validate_instance__mutmut_5'] = x_validate_instance__mutmut_5 # type: ignore # mutmut generated
mutants_x_validate_instance__mutmut['x_validate_instance__mutmut_6'] = x_validate_instance__mutmut_6 # type: ignore # mutmut generated
mutants_x_validate_instance__mutmut['x_validate_instance__mutmut_7'] = x_validate_instance__mutmut_7 # type: ignore # mutmut generated
mutants_x_validate_instance__mutmut['x_validate_instance__mutmut_8'] = x_validate_instance__mutmut_8 # type: ignore # mutmut generated
mutants_x_validate_instance__mutmut['x_validate_instance__mutmut_9'] = x_validate_instance__mutmut_9 # type: ignore # mutmut generated
mutants_x_validate_instance__mutmut['x_validate_instance__mutmut_10'] = x_validate_instance__mutmut_10 # type: ignore # mutmut generated
mutants_x_validate_instance__mutmut['x_validate_instance__mutmut_11'] = x_validate_instance__mutmut_11 # type: ignore # mutmut generated
mutants_x_validate_instance__mutmut['x_validate_instance__mutmut_12'] = x_validate_instance__mutmut_12 # type: ignore # mutmut generated
mutants_x_validate_instance__mutmut['x_validate_instance__mutmut_13'] = x_validate_instance__mutmut_13 # type: ignore # mutmut generated
mutants_x_validate_instance__mutmut['x_validate_instance__mutmut_14'] = x_validate_instance__mutmut_14 # type: ignore # mutmut generated
mutants_x_validate_instance__mutmut['x_validate_instance__mutmut_15'] = x_validate_instance__mutmut_15 # type: ignore # mutmut generated
mutants_x_validate_instance__mutmut['x_validate_instance__mutmut_16'] = x_validate_instance__mutmut_16 # type: ignore # mutmut generated
mutants_x_validate_instance__mutmut['x_validate_instance__mutmut_17'] = x_validate_instance__mutmut_17 # type: ignore # mutmut generated
mutants_x_validate_instance__mutmut['x_validate_instance__mutmut_18'] = x_validate_instance__mutmut_18 # type: ignore # mutmut generated
mutants_x_validate_instance__mutmut['x_validate_instance__mutmut_19'] = x_validate_instance__mutmut_19 # type: ignore # mutmut generated
mutants_x_validate_instance__mutmut['x_validate_instance__mutmut_20'] = x_validate_instance__mutmut_20 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_solve_uflp__mutmut)
def solve_uflp(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_orig(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_1(
    instance: UFLPInstance,
    verbose: bool = False,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_2(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(None)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_3(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_4(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(None)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_5(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = None

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_6(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print(None)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_7(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" / 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_8(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("XX=XX" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_9(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 66)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_10(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print(None)
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_11(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("XX  Hybrid LP-GRASP Solver for UFLPXX")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_12(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  hybrid lp-grasp solver for uflp")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_13(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  HYBRID LP-GRASP SOLVER FOR UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_14(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(None)
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_15(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print(None)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_16(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" / 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_17(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("XX=XX" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_18(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 66)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_19(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = None
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_20(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = None
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_21(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(None, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_22(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=None)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_23(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_24(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, )
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_25(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = None

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_26(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = None
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_27(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = None
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_28(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(None, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_29(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, None, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_30(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=None)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_31(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_32(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_33(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, )
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_34(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = None

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_35(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = None
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_36(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = None
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_37(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(None, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_38(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, None, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_39(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=None)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_40(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_41(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_42(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, )
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_43(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = None

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_44(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = None

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_45(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() + t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_46(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print(None)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_47(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" / 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_48(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("XX=XX" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_49(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 66)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_50(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(None)
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_51(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(None)
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_52(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(None)
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_53(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(None)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_54(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(None)
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_55(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(None)
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_56(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(None)
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_57(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(None)
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_58(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end + t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_59(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(None)
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_60(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end + t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_61(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(None)
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_62(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end + t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_63(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(None)
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_64(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print(None)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_65(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" / 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_66(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("XX=XX" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_67(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 66)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_68(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print(None)
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_69(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("XX\n  Customer Assignments:XX")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_70(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  customer assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_71(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  CUSTOMER ASSIGNMENTS:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_72(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(None)
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_73(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'XXCustomerXX':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_74(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_75(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'CUSTOMER':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_76(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'XXFacilityXX':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_77(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_78(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'FACILITY':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_79(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'XXService CostXX':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_80(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'service cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_81(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'SERVICE COST':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_82(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(None)
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_83(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-' / 10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_84(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'XX-XX'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_85(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*11}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_86(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-' / 10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_87(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'XX-XX'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_88(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*11}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_89(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-' / 14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_90(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'XX-XX'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_91(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*15}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_92(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(None):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_93(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = None
            sc = instance.service_costs[u][f]
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_94(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = None
            print(f"  {u:>10d}  {f:>10d}  {sc:>14,.4f}")

    return state


def x_solve_uflp__mutmut_95(
    instance: UFLPInstance,
    verbose: bool = True,
    seed: int | None = None,
) -> SolutionState:
    """
    Solve a UFLP instance using the Hybrid LP-GRASP pipeline.

    Pipeline:
        0. Instance validation
        1. LP relaxation → fractional probabilities
        2. Probabilistic construction → initial solution
        3. Best-improvement local search → local optimum
    """
    validate_instance(instance)

    if seed is not None:
        random.seed(seed)

    t0 = time.perf_counter()

    if verbose:
        print("=" * 65)
        print("  Hybrid LP-GRASP Solver for UFLP")
        print(f"  Facilities: {len(instance.facilities)}  |  "
              f"Customers: {len(instance.customers)}")
        print("=" * 65)

    # Phase 1: LP Relaxation
    t1 = time.perf_counter()
    lp_probs = solve_lp_relaxation(instance, verbose=verbose)
    t1_end = time.perf_counter()

    # Phase 2: Constructive Heuristic
    t2 = time.perf_counter()
    state = construct_solution(instance, lp_probs, verbose=verbose)
    t2_end = time.perf_counter()

    # Phase 3: Local Search
    t3 = time.perf_counter()
    state = local_search(instance, state, verbose=verbose)
    t3_end = time.perf_counter()

    t_total = time.perf_counter() - t0

    if verbose:
        print("=" * 65)
        print(f"  SOLUTION SUMMARY")
        print(f"  Total cost:       {state.total_cost:,.2f}")
        print(f"  Open facilities:  {sorted(state.open_facilities)}")
        print(f"  Num open:         {len(state.open_facilities)}/{len(instance.facilities)}")
        print(f"  ---------------------------------------------")
        print(f"  Timing:")
        print(f"    LP relaxation:   {t1_end - t1:.3f}s")
        print(f"    Construction:    {t2_end - t2:.3f}s")
        print(f"    Local search:    {t3_end - t3:.3f}s")
        print(f"    Total:           {t_total:.3f}s")
        print("=" * 65)

        # Print customer assignments
        print("\n  Customer Assignments:")
        print(f"  {'Customer':>10s}  {'Facility':>10s}  {'Service Cost':>14s}")
        print(f"  {'-'*10}  {'-'*10}  {'-'*14}")
        for u in sorted(instance.customers):
            f = state.closest_facility[u]
            sc = instance.service_costs[u][f]
            print(None)

    return state

mutants_x_solve_uflp__mutmut['_mutmut_orig'] = x_solve_uflp__mutmut_orig # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_1'] = x_solve_uflp__mutmut_1 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_2'] = x_solve_uflp__mutmut_2 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_3'] = x_solve_uflp__mutmut_3 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_4'] = x_solve_uflp__mutmut_4 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_5'] = x_solve_uflp__mutmut_5 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_6'] = x_solve_uflp__mutmut_6 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_7'] = x_solve_uflp__mutmut_7 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_8'] = x_solve_uflp__mutmut_8 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_9'] = x_solve_uflp__mutmut_9 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_10'] = x_solve_uflp__mutmut_10 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_11'] = x_solve_uflp__mutmut_11 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_12'] = x_solve_uflp__mutmut_12 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_13'] = x_solve_uflp__mutmut_13 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_14'] = x_solve_uflp__mutmut_14 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_15'] = x_solve_uflp__mutmut_15 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_16'] = x_solve_uflp__mutmut_16 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_17'] = x_solve_uflp__mutmut_17 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_18'] = x_solve_uflp__mutmut_18 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_19'] = x_solve_uflp__mutmut_19 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_20'] = x_solve_uflp__mutmut_20 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_21'] = x_solve_uflp__mutmut_21 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_22'] = x_solve_uflp__mutmut_22 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_23'] = x_solve_uflp__mutmut_23 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_24'] = x_solve_uflp__mutmut_24 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_25'] = x_solve_uflp__mutmut_25 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_26'] = x_solve_uflp__mutmut_26 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_27'] = x_solve_uflp__mutmut_27 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_28'] = x_solve_uflp__mutmut_28 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_29'] = x_solve_uflp__mutmut_29 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_30'] = x_solve_uflp__mutmut_30 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_31'] = x_solve_uflp__mutmut_31 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_32'] = x_solve_uflp__mutmut_32 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_33'] = x_solve_uflp__mutmut_33 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_34'] = x_solve_uflp__mutmut_34 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_35'] = x_solve_uflp__mutmut_35 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_36'] = x_solve_uflp__mutmut_36 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_37'] = x_solve_uflp__mutmut_37 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_38'] = x_solve_uflp__mutmut_38 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_39'] = x_solve_uflp__mutmut_39 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_40'] = x_solve_uflp__mutmut_40 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_41'] = x_solve_uflp__mutmut_41 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_42'] = x_solve_uflp__mutmut_42 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_43'] = x_solve_uflp__mutmut_43 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_44'] = x_solve_uflp__mutmut_44 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_45'] = x_solve_uflp__mutmut_45 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_46'] = x_solve_uflp__mutmut_46 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_47'] = x_solve_uflp__mutmut_47 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_48'] = x_solve_uflp__mutmut_48 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_49'] = x_solve_uflp__mutmut_49 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_50'] = x_solve_uflp__mutmut_50 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_51'] = x_solve_uflp__mutmut_51 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_52'] = x_solve_uflp__mutmut_52 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_53'] = x_solve_uflp__mutmut_53 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_54'] = x_solve_uflp__mutmut_54 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_55'] = x_solve_uflp__mutmut_55 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_56'] = x_solve_uflp__mutmut_56 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_57'] = x_solve_uflp__mutmut_57 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_58'] = x_solve_uflp__mutmut_58 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_59'] = x_solve_uflp__mutmut_59 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_60'] = x_solve_uflp__mutmut_60 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_61'] = x_solve_uflp__mutmut_61 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_62'] = x_solve_uflp__mutmut_62 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_63'] = x_solve_uflp__mutmut_63 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_64'] = x_solve_uflp__mutmut_64 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_65'] = x_solve_uflp__mutmut_65 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_66'] = x_solve_uflp__mutmut_66 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_67'] = x_solve_uflp__mutmut_67 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_68'] = x_solve_uflp__mutmut_68 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_69'] = x_solve_uflp__mutmut_69 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_70'] = x_solve_uflp__mutmut_70 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_71'] = x_solve_uflp__mutmut_71 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_72'] = x_solve_uflp__mutmut_72 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_73'] = x_solve_uflp__mutmut_73 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_74'] = x_solve_uflp__mutmut_74 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_75'] = x_solve_uflp__mutmut_75 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_76'] = x_solve_uflp__mutmut_76 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_77'] = x_solve_uflp__mutmut_77 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_78'] = x_solve_uflp__mutmut_78 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_79'] = x_solve_uflp__mutmut_79 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_80'] = x_solve_uflp__mutmut_80 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_81'] = x_solve_uflp__mutmut_81 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_82'] = x_solve_uflp__mutmut_82 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_83'] = x_solve_uflp__mutmut_83 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_84'] = x_solve_uflp__mutmut_84 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_85'] = x_solve_uflp__mutmut_85 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_86'] = x_solve_uflp__mutmut_86 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_87'] = x_solve_uflp__mutmut_87 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_88'] = x_solve_uflp__mutmut_88 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_89'] = x_solve_uflp__mutmut_89 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_90'] = x_solve_uflp__mutmut_90 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_91'] = x_solve_uflp__mutmut_91 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_92'] = x_solve_uflp__mutmut_92 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_93'] = x_solve_uflp__mutmut_93 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_94'] = x_solve_uflp__mutmut_94 # type: ignore # mutmut generated
mutants_x_solve_uflp__mutmut['x_solve_uflp__mutmut_95'] = x_solve_uflp__mutmut_95 # type: ignore # mutmut generated
mutants_x_main__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_main__mutmut)
def main() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_orig() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_1() -> None:
    """CLI entry point."""
    parser = None
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_2() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description=None,
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_3() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="XXHybrid LP-GRASP Solver for the Uncapacitated Facility Location ProblemXX",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_4() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="hybrid lp-grasp solver for the uncapacitated facility location problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_5() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="HYBRID LP-GRASP SOLVER FOR THE UNCAPACITATED FACILITY LOCATION PROBLEM",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_6() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        None, type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_7() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=None, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_8() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help=None,
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_9() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_10() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_11() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_12() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_13() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "XX--fileXX", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_14() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--FILE", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_15() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="XXPath to an OR-Library UFLP instance fileXX",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_16() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="path to an or-library uflp instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_17() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="PATH TO AN OR-LIBRARY UFLP INSTANCE FILE",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_18() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        None, type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_19() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=None, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_20() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=None,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_21() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help=None,
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_22() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_23() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_24() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_25() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_26() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "XX--n-facilitiesXX", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_27() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--N-FACILITIES", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_28() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=21,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_29() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="XXNumber of facilities for random instance (default: 20)XX",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_30() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_31() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="NUMBER OF FACILITIES FOR RANDOM INSTANCE (DEFAULT: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_32() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        None, type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_33() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=None, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_34() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=None,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_35() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help=None,
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_36() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_37() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_38() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_39() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_40() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "XX--n-customersXX", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_41() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--N-CUSTOMERS", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_42() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=31,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_43() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="XXNumber of customers for random instance (default: 30)XX",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_44() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_45() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="NUMBER OF CUSTOMERS FOR RANDOM INSTANCE (DEFAULT: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_46() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        None, type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_47() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=None, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_48() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=None,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_49() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help=None,
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_50() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_51() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_52() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_53() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_54() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "XX--seedXX", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_55() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--SEED", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_56() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=43,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_57() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="XXRandom seed (default: 42)XX",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_58() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_59() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="RANDOM SEED (DEFAULT: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_60() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        None, action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_61() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action=None,
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_62() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help=None,
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_63() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_64() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_65() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_66() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "XX--quietXX", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_67() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--QUIET", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_68() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="XXstore_trueXX",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_69() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="STORE_TRUE",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_70() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="XXSuppress verbose outputXX",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_71() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_72() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="SUPPRESS VERBOSE OUTPUT",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_73() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = None

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_74() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(None)
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_75() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = None
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_76() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(None)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_77() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(None)
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_78() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = None

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_79() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=None,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_80() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=None,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_81() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=None,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_82() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_83() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_84() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            )

    solve_uflp(instance, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_85() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(None, verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_86() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=None, seed=args.seed)


def x_main__mutmut_87() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, seed=None)


def x_main__mutmut_88() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(verbose=not args.quiet, seed=args.seed)


def x_main__mutmut_89() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, seed=args.seed)


def x_main__mutmut_90() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=not args.quiet, )


def x_main__mutmut_91() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(
        description="Hybrid LP-GRASP Solver for the Uncapacitated Facility Location Problem",
    )
    parser.add_argument(
        "--file", type=str, default=None,
        help="Path to an OR-Library UFLP instance file",
    )
    parser.add_argument(
        "--n-facilities", type=int, default=20,
        help="Number of facilities for random instance (default: 20)",
    )
    parser.add_argument(
        "--n-customers", type=int, default=30,
        help="Number of customers for random instance (default: 30)",
    )
    parser.add_argument(
        "--seed", type=int, default=42,
        help="Random seed (default: 42)",
    )
    parser.add_argument(
        "--quiet", action="store_true",
        help="Suppress verbose output",
    )

    args = parser.parse_args()

    if args.file:
        print(f"Loading OR-Library instance from: {args.file}")
        instance = parse_orlib_instance(args.file)
    else:
        print(f"Generating random instance: {args.n_facilities} facilities, "
              f"{args.n_customers} customers, seed={args.seed}")
        instance = generate_random_instance(
            n_facilities=args.n_facilities,
            n_customers=args.n_customers,
            seed=args.seed,
        )

    solve_uflp(instance, verbose=args.quiet, seed=args.seed)

mutants_x_main__mutmut['_mutmut_orig'] = x_main__mutmut_orig # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_1'] = x_main__mutmut_1 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_2'] = x_main__mutmut_2 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_3'] = x_main__mutmut_3 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_4'] = x_main__mutmut_4 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_5'] = x_main__mutmut_5 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_6'] = x_main__mutmut_6 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_7'] = x_main__mutmut_7 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_8'] = x_main__mutmut_8 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_9'] = x_main__mutmut_9 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_10'] = x_main__mutmut_10 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_11'] = x_main__mutmut_11 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_12'] = x_main__mutmut_12 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_13'] = x_main__mutmut_13 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_14'] = x_main__mutmut_14 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_15'] = x_main__mutmut_15 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_16'] = x_main__mutmut_16 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_17'] = x_main__mutmut_17 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_18'] = x_main__mutmut_18 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_19'] = x_main__mutmut_19 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_20'] = x_main__mutmut_20 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_21'] = x_main__mutmut_21 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_22'] = x_main__mutmut_22 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_23'] = x_main__mutmut_23 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_24'] = x_main__mutmut_24 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_25'] = x_main__mutmut_25 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_26'] = x_main__mutmut_26 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_27'] = x_main__mutmut_27 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_28'] = x_main__mutmut_28 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_29'] = x_main__mutmut_29 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_30'] = x_main__mutmut_30 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_31'] = x_main__mutmut_31 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_32'] = x_main__mutmut_32 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_33'] = x_main__mutmut_33 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_34'] = x_main__mutmut_34 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_35'] = x_main__mutmut_35 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_36'] = x_main__mutmut_36 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_37'] = x_main__mutmut_37 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_38'] = x_main__mutmut_38 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_39'] = x_main__mutmut_39 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_40'] = x_main__mutmut_40 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_41'] = x_main__mutmut_41 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_42'] = x_main__mutmut_42 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_43'] = x_main__mutmut_43 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_44'] = x_main__mutmut_44 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_45'] = x_main__mutmut_45 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_46'] = x_main__mutmut_46 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_47'] = x_main__mutmut_47 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_48'] = x_main__mutmut_48 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_49'] = x_main__mutmut_49 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_50'] = x_main__mutmut_50 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_51'] = x_main__mutmut_51 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_52'] = x_main__mutmut_52 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_53'] = x_main__mutmut_53 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_54'] = x_main__mutmut_54 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_55'] = x_main__mutmut_55 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_56'] = x_main__mutmut_56 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_57'] = x_main__mutmut_57 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_58'] = x_main__mutmut_58 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_59'] = x_main__mutmut_59 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_60'] = x_main__mutmut_60 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_61'] = x_main__mutmut_61 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_62'] = x_main__mutmut_62 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_63'] = x_main__mutmut_63 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_64'] = x_main__mutmut_64 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_65'] = x_main__mutmut_65 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_66'] = x_main__mutmut_66 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_67'] = x_main__mutmut_67 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_68'] = x_main__mutmut_68 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_69'] = x_main__mutmut_69 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_70'] = x_main__mutmut_70 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_71'] = x_main__mutmut_71 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_72'] = x_main__mutmut_72 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_73'] = x_main__mutmut_73 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_74'] = x_main__mutmut_74 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_75'] = x_main__mutmut_75 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_76'] = x_main__mutmut_76 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_77'] = x_main__mutmut_77 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_78'] = x_main__mutmut_78 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_79'] = x_main__mutmut_79 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_80'] = x_main__mutmut_80 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_81'] = x_main__mutmut_81 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_82'] = x_main__mutmut_82 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_83'] = x_main__mutmut_83 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_84'] = x_main__mutmut_84 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_85'] = x_main__mutmut_85 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_86'] = x_main__mutmut_86 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_87'] = x_main__mutmut_87 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_88'] = x_main__mutmut_88 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_89'] = x_main__mutmut_89 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_90'] = x_main__mutmut_90 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_91'] = x_main__mutmut_91 # type: ignore # mutmut generated


if __name__ == "__main__":
    main()
